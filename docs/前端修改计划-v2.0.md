# NextProject 前端修改计划 v2.0

## 文档信息
- **版本**：2.0.0
- **创建日期**：2025-03
- **预估工期**：2.5 个月（3 个 Phase）
- **技术栈**：Vue 3 + TypeScript + Vite + Pinia + Element Plus

---

## 一、技术选型

### 1.1 核心框架

| 技术 | 版本 | 用途 | 选择理由 |
|------|------|------|----------|
| **Vue 3** | 3.4+ | 前端框架 | 团队熟悉，生态成熟，Composition API 提升代码复用 |
| **TypeScript** | 5.3+ | 类型系统 | 提升代码质量，减少运行时错误 |
| **Vite** | 5.0+ | 构建工具 | 极速冷启动，HMR 体验好 |
| **Pinia** | 2.1+ | 状态管理 | Vue 官方推荐，比 Vuex 更简洁 |
| **Vue Router** | 4.2+ | 路由管理 | Vue 官方路由 |

### 1.2 UI 组件库

**Element Plus** - 企业级后台组件库

选择理由：
- ✅ 组件丰富（150+ 组件）
- ✅ 文档完善，中文支持好
- ✅ 主题定制方便
- ✅ TypeScript 支持完善
- ✅ 社区活跃

备选方案：
- Ant Design Vue（组件更现代，但文档不如 Element Plus）
- Naive UI（性能好，但生态较小）

### 1.3 辅助工具

| 工具 | 用途 |
|------|------|
| **Axios** | HTTP 请求库 |
| **Socket.IO Client** | WebSocket 实时通信 |
| **Monaco Editor** | 代码编辑器（查看/编辑站点代码）|
| **ECharts** | 数据可视化（监控面板）|
| **Day.js** | 日期处理（轻量级，仅 2KB）|
| **VueUse** | Vue Composition API 工具集 |
| **Lodash-es** | 工具函数库（ES Module 版本）|

### 1.4 开发工具

```json
// package.json
{
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.0.0",
    "typescript": "^5.3.3",
    "vite": "^5.0.0",
    "vue-tsc": "^1.8.27",

    // 代码规范
    "eslint": "^8.56.0",
    "eslint-plugin-vue": "^9.20.0",
    "@typescript-eslint/eslint-plugin": "^6.18.0",
    "prettier": "^3.1.1",

    // 测试
    "vitest": "^1.1.0",
    "@vue/test-utils": "^2.4.3",
    "playwright": "^1.40.0"
  }
}
```

---

## 二、项目结构

### 2.1 目录组织

```
frontend/
  ├── public/              # 静态资源
  │   ├── favicon.ico
  │   └── logo.png
  │
  ├── src/
  │   ├── assets/          # 资源文件
  │   │   ├── images/
  │   │   └── styles/
  │   │       ├── variables.scss    # 全局变量
  │   │       ├── mixins.scss       # 混入
  │   │       └── reset.scss        # 样式重置
  │   │
  │   ├── components/      # 公共组件
  │   │   ├── Layout/
  │   │   │   ├── AppHeader.vue
  │   │   │   ├── AppSidebar.vue
  │   │   │   └── AppFooter.vue
  │   │   ├── Common/
  │   │   │   ├── LoadingSpinner.vue
  │   │   │   ├── EmptyState.vue
  │   │   │   └── ErrorBoundary.vue
  │   │   └── Editor/
  │   │       ├── CodeEditor.vue    # Monaco 编辑器封装
  │   │       └── MarkdownEditor.vue
  │   │
  │   ├── views/           # 页面组件
  │   │   ├── Auth/
  │   │   │   ├── Login.vue
  │   │   │   ├── Register.vue
  │   │   │   └── ResetPassword.vue
  │   │   ├── Dashboard/
  │   │   │   └── Index.vue         # 工作台首页
  │   │   ├── Sites/
  │   │   │   ├── SiteList.vue      # 站点列表
  │   │   │   ├── SiteDetail.vue    # 站点详情
  │   │   │   ├── SiteEditor.vue    # 站点编辑器
  │   │   │   └── components/
  │   │   │       ├── TaskPanel.vue
  │   │   │       ├── VersionHistory.vue
  │   │   │       └── LivePreview.vue
  │   │   ├── Templates/
  │   │   │   ├── TemplateMarket.vue
  │   │   │   └── TemplateDetail.vue
  │   │   ├── Tasks/
  │   │   │   ├── TaskList.vue
  │   │   │   └── TaskDetail.vue
  │   │   ├── Settings/
  │   │   │   ├── Profile.vue
  │   │   │   ├── Organization.vue
  │   │   │   └── Billing.vue
  │   │   └── Monitor/
  │   │       └── Dashboard.vue     # 监控面板
  │   │
  │   ├── stores/          # Pinia 状态管理
  │   │   ├── auth.ts      # 用户认证
  │   │   ├── site.ts      # 站点管理
  │   │   ├── task.ts      # 任务状态
  │   │   └── ui.ts        # UI 状态（侧边栏折叠等）
  │   │
  │   ├── router/          # 路由配置
  │   │   ├── index.ts
  │   │   └── guards.ts    # 路由守卫
  │   │
  │   ├── api/             # API 接口
  │   │   ├── client.ts    # Axios 实例配置
  │   │   ├── auth.ts      # 认证接口
  │   │   ├── sites.ts     # 站点接口
  │   │   ├── tasks.ts     # 任务接口
  │   │   └── templates.ts # 模板接口
  │   │
  │   ├── composables/     # 组合式函数
  │   │   ├── useWebSocket.ts
  │   │   ├── useTaskLogs.ts
  │   │   └── usePermission.ts
  │   │
  │   ├── utils/           # 工具函数
  │   │   ├── format.ts    # 格式化（日期、文件大小等）
  │   │   ├── validation.ts
  │   │   └── storage.ts   # LocalStorage 封装
  │   │
  │   ├── types/           # TypeScript 类型定义
  │   │   ├── api.ts       # API 响应类型
  │   │   ├── models.ts    # 数据模型
  │   │   └── enums.ts     # 枚举
  │   │
  │   ├── App.vue          # 根组件
  │   ├── main.ts          # 入口文件
  │   └── vite-env.d.ts    # Vite 类型声明
  │
  ├── .env.development     # 开发环境变量
  ├── .env.production      # 生产环境变量
  ├── tsconfig.json        # TypeScript 配置
  ├── vite.config.ts       # Vite 配置
  ├── package.json
  └── README.md
```

---

## 三、Phase 1：基础架构（3 周）

### 3.1 项目初始化

**Step 1：创建 Vite 项目**

```bash
npm create vite@latest frontend -- --template vue-ts
cd frontend
npm install
```

**Step 2：安装依赖**

```bash
# UI 框架
npm install element-plus @element-plus/icons-vue

# 状态管理 + 路由
npm install pinia vue-router

# HTTP + WebSocket
npm install axios socket.io-client

# 工具库
npm install dayjs lodash-es
npm install @vueuse/core

# 代码编辑器
npm install monaco-editor

# 数据可视化
npm install echarts vue-echarts

# 开发工具
npm install -D @types/lodash-es
npm install -D sass
npm install -D eslint prettier
npm install -D vitest @vue/test-utils
```

**Step 3：Vite 配置**

```typescript
// vite.config.ts
import { defineConfig } from 'vite'
import vue from '@vitejs/plugin-vue'
import { resolve } from 'path'
import AutoImport from 'unplugin-auto-import/vite'
import Components from 'unplugin-vue-components/vite'
import { ElementPlusResolver } from 'unplugin-vue-components/resolvers'

export default defineConfig({
  plugins: [
    vue(),
    // Element Plus 自动导入
    AutoImport({
      resolvers: [ElementPlusResolver()],
      imports: ['vue', 'vue-router', 'pinia'],
      dts: 'src/auto-imports.d.ts',
    }),
    Components({
      resolvers: [ElementPlusResolver()],
      dts: 'src/components.d.ts',
    }),
  ],

  resolve: {
    alias: {
      '@': resolve(__dirname, 'src'),
    },
  },

  server: {
    port: 3000,
    proxy: {
      '/api': {
        target: 'http://localhost:18080',
        changeOrigin: true,
      },
      '/ws': {
        target: 'ws://localhost:18080',
        ws: true,
      },
    },
  },

  build: {
    target: 'es2015',
    outDir: 'dist',
    assetsDir: 'assets',
    sourcemap: false,

    // 代码拆分
    rollupOptions: {
      output: {
        manualChunks: {
          'element-plus': ['element-plus'],
          'monaco-editor': ['monaco-editor'],
          'echarts': ['echarts', 'vue-echarts'],
        },
      },
    },

    // 压缩配置
    minify: 'terser',
    terserOptions: {
      compress: {
        drop_console: true,
        drop_debugger: true,
      },
    },
  },
})
```

---

### 3.2 Axios 封装

```typescript
// src/api/client.ts
import axios, { AxiosInstance, AxiosRequestConfig, AxiosResponse } from 'axios'
import { ElMessage } from 'element-plus'
import { useAuthStore } from '@/stores/auth'
import router from '@/router'

// 创建 Axios 实例
const client: AxiosInstance = axios.create({
  baseURL: import.meta.env.VITE_API_BASE_URL || '/api/v2',
  timeout: 30000,
  headers: {
    'Content-Type': 'application/json',
  },
})

// 请求拦截器（自动添加 Token）
client.interceptors.request.use(
  (config: AxiosRequestConfig) => {
    const authStore = useAuthStore()
    if (authStore.token) {
      config.headers = config.headers || {}
      config.headers['Authorization'] = `Bearer ${authStore.token}`
    }
    return config
  },
  (error) => {
    return Promise.reject(error)
  }
)

// 响应拦截器（统一错误处理）
client.interceptors.response.use(
  (response: AxiosResponse) => {
    return response.data
  },
  (error) => {
    const { response } = error

    if (response) {
      switch (response.status) {
        case 401:
          ElMessage.error('登录已过期，请重新登录')
          useAuthStore().logout()
          router.push('/login')
          break
        case 403:
          ElMessage.error('无权限访问')
          break
        case 404:
          ElMessage.error('请求的资源不存在')
          break
        case 500:
          ElMessage.error('服务器错误，请稍后重试')
          break
        default:
          ElMessage.error(response.data.message || '请求失败')
      }
    } else {
      ElMessage.error('网络错误，请检查您的网络连接')
    }

    return Promise.reject(error)
  }
)

export default client
```

**API 模块示例**

```typescript
// src/api/auth.ts
import client from './client'

export interface LoginRequest {
  email: string
  password: string
}

export interface LoginResponse {
  access_token: string
  refresh_token: string
  token_type: string
  expires_in: number
}

export interface RegisterRequest {
  email: string
  password: string
  name?: string
}

export const authAPI = {
  // 登录
  login(data: LoginRequest) {
    return client.post<any, LoginResponse>('/auth/login', data)
  },

  // 注册
  register(data: RegisterRequest) {
    return client.post<any, { ok: boolean; user_id: string }>('/auth/register', data)
  },

  // 获取当前用户信息
  getCurrentUser() {
    return client.get('/auth/me')
  },

  // 刷新 Token
  refreshToken(refreshToken: string) {
    return client.post('/auth/refresh', { refresh_token: refreshToken })
  },
}
```

```typescript
// src/api/sites.ts
import client from './client'
import type { Site, SiteCreateRequest, SiteUpdateRequest } from '@/types/models'

export const sitesAPI = {
  // 获取站点列表
  list() {
    return client.get<any, { ok: boolean; sites: Site[] }>('/sites')
  },

  // 创建站点
  create(data: SiteCreateRequest) {
    return client.post<any, { ok: boolean; site: Site }>('/sites', data)
  },

  // 获取站点详情
  get(siteId: string) {
    return client.get<any, { ok: boolean; site: Site }>(`/sites/${siteId}`)
  },

  // 更新站点
  update(siteId: string, data: SiteUpdateRequest) {
    return client.patch<any, { ok: boolean; site: Site }>(`/sites/${siteId}`, data)
  },

  // 删除站点
  delete(siteId: string) {
    return client.delete(`/sites/${siteId}`)
  },

  // 启动站点
  start(siteId: string) {
    return client.post(`/sites/${siteId}/start`)
  },

  // 停止站点
  stop(siteId: string) {
    return client.post(`/sites/${siteId}/stop`)
  },
}
```

---

### 3.3 状态管理（Pinia）

```typescript
// src/stores/auth.ts
import { defineStore } from 'pinia'
import { authAPI } from '@/api/auth'
import type { User } from '@/types/models'

interface AuthState {
  token: string | null
  refreshToken: string | null
  user: User | null
}

export const useAuthStore = defineStore('auth', {
  state: (): AuthState => ({
    token: localStorage.getItem('access_token'),
    refreshToken: localStorage.getItem('refresh_token'),
    user: null,
  }),

  getters: {
    isAuthenticated: (state) => !!state.token,
  },

  actions: {
    // 登录
    async login(email: string, password: string) {
      const response = await authAPI.login({ email, password })

      this.token = response.access_token
      this.refreshToken = response.refresh_token

      localStorage.setItem('access_token', response.access_token)
      localStorage.setItem('refresh_token', response.refresh_token)

      // 获取用户信息
      await this.fetchUser()
    },

    // 注册
    async register(email: string, password: string, name?: string) {
      await authAPI.register({ email, password, name })
      // 注册成功后自动登录
      await this.login(email, password)
    },

    // 获取用户信息
    async fetchUser() {
      const response = await authAPI.getCurrentUser()
      this.user = response
    },

    // 登出
    logout() {
      this.token = null
      this.refreshToken = null
      this.user = null

      localStorage.removeItem('access_token')
      localStorage.removeItem('refresh_token')
    },

    // 刷新 Token
    async refresh() {
      if (!this.refreshToken) {
        this.logout()
        return
      }

      try {
        const response = await authAPI.refreshToken(this.refreshToken)
        this.token = response.access_token
        localStorage.setItem('access_token', response.access_token)
      } catch (error) {
        this.logout()
      }
    },
  },
})
```

```typescript
// src/stores/site.ts
import { defineStore } from 'pinia'
import { sitesAPI } from '@/api/sites'
import type { Site } from '@/types/models'

interface SiteState {
  sites: Site[]
  currentSite: Site | null
  loading: boolean
}

export const useSiteStore = defineStore('site', {
  state: (): SiteState => ({
    sites: [],
    currentSite: null,
    loading: false,
  }),

  getters: {
    runningSites: (state) => state.sites.filter((s) => s.status === 'running'),
  },

  actions: {
    // 获取站点列表
    async fetchSites() {
      this.loading = true
      try {
        const response = await sitesAPI.list()
        this.sites = response.sites
      } finally {
        this.loading = false
      }
    },

    // 创建站点
    async createSite(data: { name: string; template_id?: string }) {
      const response = await sitesAPI.create(data)
      this.sites.unshift(response.site)
      return response.site
    },

    // 启动站点
    async startSite(siteId: string) {
      await sitesAPI.start(siteId)
      const site = this.sites.find((s) => s.site_id === siteId)
      if (site) {
        site.status = 'running'
      }
    },

    // 停止站点
    async stopSite(siteId: string) {
      await sitesAPI.stop(siteId)
      const site = this.sites.find((s) => s.site_id === siteId)
      if (site) {
        site.status = 'stopped'
      }
    },

    // 删除站点
    async deleteSite(siteId: string) {
      await sitesAPI.delete(siteId)
      this.sites = this.sites.filter((s) => s.site_id !== siteId)
    },
  },
})
```

---

### 3.4 路由配置

```typescript
// src/router/index.ts
import { createRouter, createWebHistory, RouteRecordRaw } from 'vue-router'
import { useAuthStore } from '@/stores/auth'

const routes: RouteRecordRaw[] = [
  {
    path: '/login',
    name: 'Login',
    component: () => import('@/views/Auth/Login.vue'),
    meta: { requiresAuth: false },
  },
  {
    path: '/register',
    name: 'Register',
    component: () => import('@/views/Auth/Register.vue'),
    meta: { requiresAuth: false },
  },
  {
    path: '/',
    component: () => import('@/components/Layout/AppLayout.vue'),
    meta: { requiresAuth: true },
    children: [
      {
        path: '',
        name: 'Dashboard',
        component: () => import('@/views/Dashboard/Index.vue'),
      },
      {
        path: 'sites',
        name: 'SiteList',
        component: () => import('@/views/Sites/SiteList.vue'),
      },
      {
        path: 'sites/:id',
        name: 'SiteDetail',
        component: () => import('@/views/Sites/SiteDetail.vue'),
      },
      {
        path: 'sites/:id/edit',
        name: 'SiteEditor',
        component: () => import('@/views/Sites/SiteEditor.vue'),
      },
      {
        path: 'templates',
        name: 'TemplateMarket',
        component: () => import('@/views/Templates/TemplateMarket.vue'),
      },
      {
        path: 'tasks',
        name: 'TaskList',
        component: () => import('@/views/Tasks/TaskList.vue'),
      },
      {
        path: 'settings',
        name: 'Settings',
        component: () => import('@/views/Settings/Profile.vue'),
      },
    ],
  },
]

const router = createRouter({
  history: createWebHistory(),
  routes,
})

// 路由守卫：认证检查
router.beforeEach((to, from, next) => {
  const authStore = useAuthStore()

  if (to.meta.requiresAuth && !authStore.isAuthenticated) {
    next({ name: 'Login', query: { redirect: to.fullPath } })
  } else {
    next()
  }
})

export default router
```

---

## 四、Phase 2：核心功能（5 周）

### 4.1 站点管理页面

#### SiteList.vue（站点列表）

```vue
<template>
  <div class="site-list">
    <el-row :gutter="20" class="header">
      <el-col :span="12">
        <h1>我的站点</h1>
      </el-col>
      <el-col :span="12" class="text-right">
        <el-button type="primary" @click="showCreateDialog = true">
          <el-icon><Plus /></el-icon>
          创建站点
        </el-button>
      </el-col>
    </el-row>

    <!-- 筛选器 -->
    <el-card class="filter-card" shadow="never">
      <el-form :inline="true">
        <el-form-item label="状态">
          <el-select v-model="filter.status" placeholder="全部状态">
            <el-option label="全部" value="" />
            <el-option label="运行中" value="running" />
            <el-option label="已停止" value="stopped" />
          </el-select>
        </el-form-item>
        <el-form-item label="搜索">
          <el-input
            v-model="filter.search"
            placeholder="站点名称或 ID"
            clearable
          />
        </el-form-item>
      </el-form>
    </el-card>

    <!-- 站点卡片 -->
    <el-row :gutter="20" v-loading="siteStore.loading">
      <el-col
        :span="8"
        v-for="site in filteredSites"
        :key="site.site_id"
        class="site-card-col"
      >
        <el-card class="site-card" shadow="hover">
          <template #header>
            <div class="card-header">
              <span class="site-name">{{ site.name }}</span>
              <el-tag :type="site.status === 'running' ? 'success' : 'info'">
                {{ site.status }}
              </el-tag>
            </div>
          </template>

          <div class="site-info">
            <p><strong>ID:</strong> {{ site.site_id }}</p>
            <p><strong>端口:</strong> {{ site.port }}</p>
            <p><strong>创建时间:</strong> {{ formatDate(site.created_at) }}</p>
          </div>

          <div class="site-actions">
            <el-button-group>
              <el-button
                size="small"
                :type="site.status === 'running' ? 'danger' : 'success'"
                @click="toggleSiteStatus(site)"
              >
                {{ site.status === 'running' ? '停止' : '启动' }}
              </el-button>
              <el-button
                size="small"
                @click="previewSite(site)"
                :disabled="site.status !== 'running'"
              >
                预览
              </el-button>
              <el-button
                size="small"
                type="primary"
                @click="editSite(site)"
              >
                编辑
              </el-button>
            </el-button-group>

            <el-popconfirm
              title="确定删除这个站点吗？"
              @confirm="deleteSite(site)"
            >
              <template #reference>
                <el-button size="small" type="danger" plain>
                  删除
                </el-button>
              </template>
            </el-popconfirm>
          </div>
        </el-card>
      </el-col>
    </el-row>

    <!-- 创建站点对话框 -->
    <el-dialog
      v-model="showCreateDialog"
      title="创建新站点"
      width="500px"
    >
      <el-form :model="createForm" label-width="100px">
        <el-form-item label="站点名称" required>
          <el-input v-model="createForm.name" placeholder="我的网站" />
        </el-form-item>
        <el-form-item label="选择模板">
          <el-select v-model="createForm.template_id" placeholder="空白模板">
            <el-option label="空白模板" value="" />
            <el-option label="博客模板" value="blog" />
            <el-option label="仪表盘" value="dashboard" />
          </el-select>
        </el-form-item>
      </el-form>

      <template #footer>
        <el-button @click="showCreateDialog = false">取消</el-button>
        <el-button type="primary" @click="createSite" :loading="creating">
          创建
        </el-button>
      </template>
    </el-dialog>
  </div>
</template>

<script setup lang="ts">
import { ref, computed, onMounted } from 'vue'
import { useRouter } from 'vue-router'
import { useSiteStore } from '@/stores/site'
import { ElMessage } from 'element-plus'
import { formatDate } from '@/utils/format'
import type { Site } from '@/types/models'

const router = useRouter()
const siteStore = useSiteStore()

const filter = ref({
  status: '',
  search: '',
})

const showCreateDialog = ref(false)
const creating = ref(false)

const createForm = ref({
  name: '',
  template_id: '',
})

const filteredSites = computed(() => {
  let sites = siteStore.sites

  if (filter.value.status) {
    sites = sites.filter((s) => s.status === filter.value.status)
  }

  if (filter.value.search) {
    const query = filter.value.search.toLowerCase()
    sites = sites.filter(
      (s) =>
        s.name.toLowerCase().includes(query) ||
        s.site_id.toLowerCase().includes(query)
    )
  }

  return sites
})

onMounted(() => {
  siteStore.fetchSites()
})

const toggleSiteStatus = async (site: Site) => {
  try {
    if (site.status === 'running') {
      await siteStore.stopSite(site.site_id)
      ElMessage.success('站点已停止')
    } else {
      await siteStore.startSite(site.site_id)
      ElMessage.success('站点已启动')
    }
  } catch (error) {
    ElMessage.error('操作失败')
  }
}

const previewSite = (site: Site) => {
  window.open(site.preview_url, '_blank')
}

const editSite = (site: Site) => {
  router.push({ name: 'SiteEditor', params: { id: site.site_id } })
}

const deleteSite = async (site: Site) => {
  try {
    await siteStore.deleteSite(site.site_id)
    ElMessage.success('站点已删除')
  } catch (error) {
    ElMessage.error('删除失败')
  }
}

const createSite = async () => {
  if (!createForm.value.name) {
    ElMessage.warning('请输入站点名称')
    return
  }

  creating.value = true
  try {
    await siteStore.createSite(createForm.value)
    ElMessage.success('站点创建成功')
    showCreateDialog.value = false
    createForm.value = { name: '', template_id: '' }
  } catch (error) {
    ElMessage.error('创建失败')
  } finally {
    creating.value = false
  }
}
</script>

<style scoped lang="scss">
.site-list {
  padding: 20px;

  .header {
    margin-bottom: 20px;
    align-items: center;
  }

  .filter-card {
    margin-bottom: 20px;
  }

  .site-card-col {
    margin-bottom: 20px;
  }

  .site-card {
    .card-header {
      display: flex;
      justify-content: space-between;
      align-items: center;

      .site-name {
        font-size: 16px;
        font-weight: bold;
      }
    }

    .site-info {
      margin-bottom: 15px;
      p {
        margin: 5px 0;
        color: #606266;
      }
    }

    .site-actions {
      display: flex;
      justify-content: space-between;
    }
  }
}
</style>
```

---

### 4.2 实时日志（WebSocket）

#### useWebSocket 组合式函数

```typescript
// src/composables/useWebSocket.ts
import { ref, onUnmounted } from 'vue'
import { io, Socket } from 'socket.io-client'
import { useAuthStore } from '@/stores/auth'

export function useWebSocket(namespace: string = '/') {
  const socket = ref<Socket | null>(null)
  const connected = ref(false)
  const authStore = useAuthStore()

  const connect = () => {
    socket.value = io(`ws://localhost:18080${namespace}`, {
      auth: {
        token: authStore.token,
      },
      transports: ['websocket'],
    })

    socket.value.on('connect', () => {
      connected.value = true
      console.log('WebSocket 已连接')
    })

    socket.value.on('disconnect', () => {
      connected.value = false
      console.log('WebSocket 已断开')
    })

    socket.value.on('error', (error) => {
      console.error('WebSocket 错误:', error)
    })
  }

  const disconnect = () => {
    if (socket.value) {
      socket.value.disconnect()
      socket.value = null
    }
  }

  const emit = (event: string, data: any) => {
    if (socket.value && connected.value) {
      socket.value.emit(event, data)
    }
  }

  const on = (event: string, callback: (...args: any[]) => void) => {
    if (socket.value) {
      socket.value.on(event, callback)
    }
  }

  const off = (event: string, callback?: (...args: any[]) => void) => {
    if (socket.value) {
      socket.value.off(event, callback)
    }
  }

  onUnmounted(() => {
    disconnect()
  })

  return {
    socket,
    connected,
    connect,
    disconnect,
    emit,
    on,
    off,
  }
}
```

#### 任务日志组件

```vue
<!-- src/components/TaskLogs.vue -->
<template>
  <div class="task-logs">
    <el-card>
      <template #header>
        <div class="header">
          <span>任务日志</span>
          <div>
            <el-button size="small" @click="clearLogs">清空</el-button>
            <el-button
              size="small"
              :type="autoScroll ? 'primary' : 'default'"
              @click="autoScroll = !autoScroll"
            >
              自动滚动
            </el-button>
          </div>
        </div>
      </template>

      <div class="log-container" ref="logContainerRef">
        <div
          v-for="(log, index) in logs"
          :key="index"
          :class="['log-line', `log-${log.level.toLowerCase()}`]"
        >
          <span class="log-time">{{ log.ts }}</span>
          <span class="log-level">[{{ log.level }}]</span>
          <span class="log-message">{{ log.line }}</span>
        </div>

        <div v-if="logs.length === 0" class="empty-state">
          暂无日志
        </div>
      </div>
    </el-card>
  </div>
</template>

<script setup lang="ts">
import { ref, watch, nextTick, onMounted, onUnmounted } from 'vue'
import { useWebSocket } from '@/composables/useWebSocket'

interface LogEntry {
  ts: string
  level: string
  line: string
}

const props = defineProps<{
  taskId: string
}>()

const logs = ref<LogEntry[]>([])
const autoScroll = ref(true)
const logContainerRef = ref<HTMLElement | null>(null)

const { connect, disconnect, on, off } = useWebSocket('/ws')

onMounted(() => {
  connect()

  // 监听日志事件
  on(`task:${props.taskId}:log`, (log: LogEntry) => {
    logs.value.push(log)

    if (autoScroll.value) {
      scrollToBottom()
    }
  })
})

onUnmounted(() => {
  off(`task:${props.taskId}:log`)
  disconnect()
})

const scrollToBottom = async () => {
  await nextTick()
  if (logContainerRef.value) {
    logContainerRef.value.scrollTop = logContainerRef.value.scrollHeight
  }
}

const clearLogs = () => {
  logs.value = []
}

watch(() => props.taskId, () => {
  logs.value = []
})
</script>

<style scoped lang="scss">
.task-logs {
  .header {
    display: flex;
    justify-content: space-between;
    align-items: center;
  }

  .log-container {
    height: 500px;
    overflow-y: auto;
    background: #1e1e1e;
    color: #d4d4d4;
    padding: 10px;
    font-family: 'Consolas', 'Monaco', monospace;
    font-size: 13px;
    line-height: 1.5;

    .log-line {
      margin-bottom: 4px;

      .log-time {
        color: #808080;
      }

      .log-level {
        margin: 0 8px;
        font-weight: bold;
      }

      &.log-info .log-level {
        color: #4fc3f7;
      }

      &.log-warn .log-level {
        color: #ffa726;
      }

      &.log-error .log-level {
        color: #ef5350;
      }
    }

    .empty-state {
      color: #808080;
      text-align: center;
      padding: 50px 0;
    }
  }
}
</style>
```

---

### 4.3 Monaco 编辑器集成

```vue
<!-- src/components/Editor/CodeEditor.vue -->
<template>
  <div class="code-editor" ref="editorContainer"></div>
</template>

<script setup lang="ts">
import { ref, onMounted, onUnmounted, watch } from 'vue'
import * as monaco from 'monaco-editor'

const props = defineProps<{
  modelValue: string
  language?: string
  readonly?: boolean
  theme?: string
}>()

const emit = defineEmits<{
  (e: 'update:modelValue', value: string): void
}>()

const editorContainer = ref<HTMLElement | null>(null)
let editor: monaco.editor.IStandaloneCodeEditor | null = null

onMounted(() => {
  if (editorContainer.value) {
    editor = monaco.editor.create(editorContainer.value, {
      value: props.modelValue,
      language: props.language || 'javascript',
      theme: props.theme || 'vs-dark',
      readOnly: props.readonly || false,
      automaticLayout: true,
      minimap: { enabled: true },
      fontSize: 14,
      lineNumbers: 'on',
      scrollBeyondLastLine: false,
      wordWrap: 'on',
    })

    // 监听内容变化
    editor.onDidChangeModelContent(() => {
      const value = editor?.getValue() || ''
      emit('update:modelValue', value)
    })
  }
})

onUnmounted(() => {
  editor?.dispose()
})

// 监听外部值变化
watch(() => props.modelValue, (newValue) => {
  if (editor && editor.getValue() !== newValue) {
    editor.setValue(newValue)
  }
})

// 监听语言变化
watch(() => props.language, (newLanguage) => {
  if (editor) {
    const model = editor.getModel()
    if (model) {
      monaco.editor.setModelLanguage(model, newLanguage || 'javascript')
    }
  }
})
</script>

<style scoped>
.code-editor {
  width: 100%;
  height: 100%;
  min-height: 400px;
}
</style>
```

---

## 五、Phase 3：高级功能（3 周）

### 5.1 模板市场

补充说明：模板市场「创建站点」联调时发现前端曾调用旧路径 `/api/v2/templates/use`，后端实际已提供的是 `POST /api/v2/templates/sites/from-template`，返回体主字段为 `site_id`。现网实现已按后端契约修正，并在前端 API 层兼容为 `response.site.site_id` 供页面跳转复用。

```vue
<!-- src/views/Templates/TemplateMarket.vue -->
<template>
  <div class="template-market">
    <h1>模板市场</h1>

    <!-- 分类筛选 -->
    <el-tabs v-model="activeCategory" @tab-change="fetchTemplates">
      <el-tab-pane label="全部" name="all" />
      <el-tab-pane label="博客" name="blog" />
      <el-tab-pane label="仪表盘" name="dashboard" />
      <el-tab-pane label="电商" name="ecommerce" />
      <el-tab-pane label="落地页" name="landing" />
    </el-tabs>

    <!-- 模板卡片 -->
    <el-row :gutter="20" v-loading="loading">
      <el-col
        :span="6"
        v-for="template in templates"
        :key="template.id"
        class="template-card-col"
      >
        <el-card class="template-card" shadow="hover">
          <div class="template-thumbnail">
            <img :src="template.thumbnail_url" :alt="template.name" />
          </div>

          <h3>{{ template.name }}</h3>
          <p class="description">{{ template.description }}</p>

          <div class="meta">
            <el-rate
              v-model="template.rating"
              disabled
              show-score
              score-template="{value}"
            />
            <span class="usage-count">
              <el-icon><User /></el-icon>
              {{ template.usage_count }}
            </span>
          </div>

          <el-button
            type="primary"
            block
            @click="useTemplate(template)"
          >
            使用此模板
          </el-button>
        </el-card>
      </el-col>
    </el-row>

    <!-- 使用模板对话框 -->
    <el-dialog v-model="showUseDialog" title="从模板创建站点" width="400px">
      <el-form>
        <el-form-item label="站点名称" required>
          <el-input v-model="siteName" placeholder="我的博客" />
        </el-form-item>
      </el-form>

      <template #footer>
        <el-button @click="showUseDialog = false">取消</el-button>
        <el-button type="primary" @click="createFromTemplate">
          创建
        </el-button>
      </template>
    </el-dialog>
  </div>
</template>

<script setup lang="ts">
import { ref, onMounted } from 'vue'
import { useRouter } from 'vue-router'
import { templatesAPI } from '@/api/templates'
import { ElMessage } from 'element-plus'

const router = useRouter()

const activeCategory = ref('all')
const templates = ref([])
const loading = ref(false)

const showUseDialog = ref(false)
const selectedTemplate = ref(null)
const siteName = ref('')

onMounted(() => {
  fetchTemplates()
})

const fetchTemplates = async () => {
  loading.value = true
  try {
    const response = await templatesAPI.list({
      category: activeCategory.value === 'all' ? undefined : activeCategory.value,
    })
    templates.value = response.templates
  } finally {
    loading.value = false
  }
}

const useTemplate = (template) => {
  selectedTemplate.value = template
  siteName.value = ''
  showUseDialog.value = true
}

const createFromTemplate = async () => {
  if (!siteName.value) {
    ElMessage.warning('请输入站点名称')
    return
  }

  try {
    const response = await templatesAPI.createSiteFromTemplate({
      template_id: selectedTemplate.value.id,
      site_name: siteName.value,
    })

    ElMessage.success('站点创建成功')
    showUseDialog.value = false

    router.push({ name: 'SiteEditor', params: { id: response.site.site_id } })
  } catch (error) {
    ElMessage.error('创建失败')
  }
}
</script>
```

---

### 5.2 监控面板（ECharts）

```vue
<!-- src/views/Monitor/Dashboard.vue -->
<template>
  <div class="monitor-dashboard">
    <h1>系统监控</h1>

    <el-row :gutter="20">
      <!-- 关键指标卡片 -->
      <el-col :span="6">
        <el-card class="metric-card">
          <div class="metric">
            <div class="icon">
              <el-icon size="40" color="#409eff"><Monitor /></el-icon>
            </div>
            <div class="value">
              <span class="number">{{ metrics.activeSites }}</span>
              <span class="label">活跃站点</span>
            </div>
          </div>
        </el-card>
      </el-col>

      <el-col :span="6">
        <el-card class="metric-card">
          <div class="metric">
            <div class="icon">
              <el-icon size="40" color="#67c23a"><Clock /></el-icon>
            </div>
            <div class="value">
              <span class="number">{{ metrics.runningTasks }}</span>
              <span class="label">运行中任务</span>
            </div>
          </div>
        </el-card>
      </el-col>

      <el-col :span="6">
        <el-card class="metric-card">
          <div class="metric">
            <div class="icon">
              <el-icon size="40" color="#e6a23c"><Cpu /></el-icon>
            </div>
            <div class="value">
              <span class="number">{{ metrics.cpuUsage }}%</span>
              <span class="label">CPU 使用率</span>
            </div>
          </div>
        </el-card>
      </el-col>

      <el-col :span="6">
        <el-card class="metric-card">
          <div class="metric">
            <div class="icon">
              <el-icon size="40" color="#f56c6c"><Memory /></el-icon>
            </div>
            <div class="value">
              <span class="number">{{ metrics.memoryUsage }}%</span>
              <span class="label">内存使用率</span>
            </div>
          </div>
        </el-card>
      </el-col>
    </el-row>

    <!-- ECharts 图表 -->
    <el-row :gutter="20" style="margin-top: 20px">
      <el-col :span="12">
        <el-card>
          <template #header>QPS 趋势</template>
          <v-chart :option="qpsOption" style="height: 300px" />
        </el-card>
      </el-col>

      <el-col :span="12">
        <el-card>
          <template #header>响应时间（P95）</template>
          <v-chart :option="latencyOption" style="height: 300px" />
        </el-card>
      </el-col>
    </el-row>
  </div>
</template>

<script setup lang="ts">
import { ref, onMounted } from 'vue'
import VChart from 'vue-echarts'
import { use } from 'echarts/core'
import { CanvasRenderer } from 'echarts/renderers'
import { LineChart } from 'echarts/charts'
import {
  TitleComponent,
  TooltipComponent,
  LegendComponent,
  GridComponent,
} from 'echarts/components'

use([
  CanvasRenderer,
  LineChart,
  TitleComponent,
  TooltipComponent,
  LegendComponent,
  GridComponent,
])

const metrics = ref({
  activeSites: 0,
  runningTasks: 0,
  cpuUsage: 0,
  memoryUsage: 0,
})

const qpsOption = ref({
  tooltip: {
    trigger: 'axis',
  },
  xAxis: {
    type: 'category',
    data: [],
  },
  yAxis: {
    type: 'value',
  },
  series: [
    {
      name: 'QPS',
      type: 'line',
      data: [],
      smooth: true,
    },
  ],
})

const latencyOption = ref({
  tooltip: {
    trigger: 'axis',
  },
  xAxis: {
    type: 'category',
    data: [],
  },
  yAxis: {
    type: 'value',
    axisLabel: {
      formatter: '{value} ms',
    },
  },
  series: [
    {
      name: 'P95 Latency',
      type: 'line',
      data: [],
      smooth: true,
    },
  ],
})

onMounted(() => {
  // 模拟数据（实际应从 Prometheus API 获取）
  metrics.value = {
    activeSites: 42,
    runningTasks: 8,
    cpuUsage: 35,
    memoryUsage: 62,
  }

  // 模拟时间序列数据
  const times = []
  const qpsData = []
  const latencyData = []

  for (let i = 0; i < 20; i++) {
    times.push(`${i}:00`)
    qpsData.push(Math.floor(Math.random() * 100))
    latencyData.push(Math.floor(Math.random() * 200))
  }

  qpsOption.value.xAxis.data = times
  qpsOption.value.series[0].data = qpsData

  latencyOption.value.xAxis.data = times
  latencyOption.value.series[0].data = latencyData
})
</script>

<style scoped lang="scss">
.monitor-dashboard {
  padding: 20px;

  .metric-card {
    .metric {
      display: flex;
      align-items: center;
      gap: 20px;

      .value {
        display: flex;
        flex-direction: column;

        .number {
          font-size: 32px;
          font-weight: bold;
        }

        .label {
          color: #909399;
          font-size: 14px;
        }
      }
    }
  }
}
</style>
```

---

## 六、性能优化

### 6.1 代码分割

```typescript
// vite.config.ts
export default defineConfig({
  build: {
    rollupOptions: {
      output: {
        manualChunks: {
          // 第三方库
          'element-plus': ['element-plus'],
          'monaco-editor': ['monaco-editor'],
          'echarts': ['echarts', 'vue-echarts'],

          // 按路由分割
          auth: ['./src/views/Auth/Login.vue', './src/views/Auth/Register.vue'],
          sites: ['./src/views/Sites/SiteList.vue', './src/views/Sites/SiteEditor.vue'],
        },
      },
    },
  },
})
```

### 6.2 图片懒加载

```vue
<template>
  <img v-lazy="imageUrl" alt="Thumbnail" />
</template>

<script setup>
import { directive as vLazy } from 'vue3-lazy'
</script>
```

### 6.3 虚拟滚动（大列表优化）

```bash
npm install vue-virtual-scroller
```

```vue
<template>
  <RecycleScroller
    :items="sites"
    :item-size="100"
    key-field="site_id"
    v-slot="{ item }"
  >
    <SiteCard :site="item" />
  </RecycleScroller>
</template>
```

---

## 七、测试策略

### 7.1 单元测试（Vitest）

```typescript
// tests/components/SiteCard.spec.ts
import { describe, it, expect } from 'vitest'
import { mount } from '@vue/test-utils'
import SiteCard from '@/components/SiteCard.vue'

describe('SiteCard', () => {
  it('renders site name', () => {
    const wrapper = mount(SiteCard, {
      props: {
        site: {
          site_id: 'test-1',
          name: 'Test Site',
          status: 'running',
        },
      },
    })

    expect(wrapper.text()).toContain('Test Site')
  })

  it('emits delete event when delete button clicked', async () => {
    const wrapper = mount(SiteCard, {
      props: { site: { /* ... */ } },
    })

    await wrapper.find('.delete-btn').trigger('click')
    expect(wrapper.emitted('delete')).toBeTruthy()
  })
})
```

### 7.2 E2E 测试（Playwright）

```typescript
// tests/e2e/site-creation.spec.ts
import { test, expect } from '@playwright/test'

test('create site from template', async ({ page }) => {
  // 1. 登录
  await page.goto('http://localhost:3000/login')
  await page.fill('input[name="email"]', 'test@example.com')
  await page.fill('input[name="password"]', 'password123')
  await page.click('button[type="submit"]')

  // 2. 进入模板市场
  await page.click('text=模板市场')
  await expect(page).toHaveURL(/templates/)

  // 3. 选择模板
  await page.click('.template-card:first-child button')

  // 4. 输入站点名称
  await page.fill('input[placeholder="我的博客"]', 'My Test Site')
  await page.click('text=创建')

  // 5. 验证跳转到编辑器
  await expect(page).toHaveURL(/sites\/.+\/edit/)
})
```

---

## 八、部署配置

### 8.1 环境变量

```bash
# .env.production
VITE_API_BASE_URL=https://api.nextproject.com/api/v2
VITE_WS_URL=wss://api.nextproject.com/ws
```

### 8.2 Nginx 配置

```nginx
server {
    listen 80;
    server_name nextproject.com;

    root /var/www/frontend/dist;
    index index.html;

    # Gzip 压缩
    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml application/xml+rss text/javascript;

    location / {
        try_files $uri $uri/ /index.html;
    }

    # 静态资源缓存
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # API 代理
    location /api/ {
        proxy_pass http://localhost:18080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    # WebSocket 代理
    location /ws/ {
        proxy_pass http://localhost:18080;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "Upgrade";
    }
}
```

---

## 九、迁移检查清单

### Phase 1 完成标准
- [ ] 项目初始化完成，Vite + Vue 3 + TypeScript 正常运行
- [ ] Axios 封装完成，请求/响应拦截器正常
- [ ] Pinia 状态管理可用，auth/site store 测试通过
- [ ] Vue Router 路由守卫正常
- [ ] Element Plus 主题定制完成

### Phase 2 完成标准
- [ ] 站点列表页面完成（增删改查）
- [ ] WebSocket 实时日志正常推送
- [ ] Monaco 编辑器集成成功
- [ ] 任务管理页面完成
- [ ] 响应式布局（适配 1920px、1366px、768px）

### Phase 3 完成标准
- [ ] 模板市场上线
- [ ] 监控面板 ECharts 图表正常
- [ ] 单元测试覆盖率 > 60%
- [ ] E2E 测试通过（关键流程）
- [ ] 生产环境构建成功，Bundle 分析无异常

---

**文档版本**：2.0.0
**编写日期**：2025-03
**维护者**：前端团队
