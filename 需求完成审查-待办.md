# 需求完成审查 - 待办

审查日期：2026-06-23

审查范围：
- `需求文档1.0.0.md`
- `需求文档2.0.0.md`

判定口径：缺少代码实现、未接入主流程、只有设计文档或仅完成基础骨架但未形成闭环的条目归入本文件。

## 1.0.0 待办项

### Apollo 部署闭环

- `deploy_apollo` 目前只创建任务并返回“已接受”，尚未真正执行 Dockerfile 生成、镜像构建、推送 Harbor、登录 Apollo、调用 `deployByImage`、清理镜像等流程。
- 需要把 `SiteDeployConfig` 中的 Apollo 与 Harbor 配置接入实际部署执行。
- 需要补充部署失败日志、重试、超时、回滚或清理策略。

### 任务取消与恢复

- 任务取消目前主要更新状态并尝试 `celery revoke`，尚未在 `run_shell_command` 内按取消标记主动终止子进程。
- `celery_task_id` 字段存在，但入队时没有保存 Celery 返回的任务 ID，取消运行中任务的可靠性不足。
- 服务启动时会恢复 `running` 站点，但尚未把重启前遗留的 `queued/running` 任务统一标记为 `failed`。

### Playwright 冒烟测试可靠性

- 冒烟脚本仍访问旧页面和旧接口，例如 `/home`、`/site-editor/{site_id}`、`/api/adjust-site`。
- 当前主前端已切换到鉴权后的 Vue 路由，测试脚本需要处理登录态或改为直接验证 v2 前端流程。
- 需要补充截图、报告索引、失败详情和前端可视化入口。

### 安全存储

- `UserLLMProvider.api_key` 已加密，但 `AppConfig.llm_api_key`、`UserConfig.llm_api_key`、`codex_access_token`、`claude_api_key`、`SiteDeployConfig.login_password` 等仍是明文字段。
- 需要统一密钥字段加密、脱敏返回、迁移脚本和回滚策略。

### 日志清理与运维

- 任务日志已持久化，但缺少日志过期、归档、压缩和清理策略。
- `/shared/task_artifacts` 缺少生命周期管理。

## 2.0.0 待办项

### 微服务拆分与 API 网关

- 当前仍是单体 FastAPI 应用，尚未拆分为 `api-gateway`、`site-manager`、`task-scheduler`、`ai-proxy`、`deploy-engine`。
- API 网关层的认证集中化、限流、流量治理还未完成。
- `/api/v2` 已存在，但旧 `/api` 兼容接口仍保留，需要明确迁移和下线策略。

### 完整多租户与权限

- 已有用户、组织、成员和角色基础表，但还缺少组织 CRUD、成员邀请、团队层级、租户切换、细粒度资源授权。
- `Organization -> Team -> User` 的 Team 层尚未实现。
- 角色权限目前只覆盖少量接口，需要扩展到所有站点、项目、模板、部署、配置和任务操作。

### 数据库与任务队列生产化

- Docker Compose 默认使用 PostgreSQL，但应用配置默认值仍是 SQLite，需要明确本地、测试、生产配置边界。
- 需求中的 `task_queue` 持久化表尚未实现。
- Celery 入队失败会被静默吞掉，任务可能停留在 `queued`。
- Celery 任务 ID 未写回数据库，取消、追踪和重试不可观测。
- 缺少优先级、延迟调度、重试次数、死信队列、队列长度真实采集和 Worker 扩缩容策略。

### 子网站容器化与路由

- `ContainerService` 存在，但站点启动主流程仍使用本机子进程。
- 每站点独立 Docker 容器、资源限制、健康检查、生命周期管理尚未接入 `start_site/stop_site`。
- Traefik 动态路由和域名路由未实现。
- 容器预热池、空闲回收和按需唤醒未实现。

### 模板市场完整能力

- 已有公开模板列表和从模板创建站点，但缺少模板上传、自定义模板保存、分享、Fork、审核、缩略图管理和模板版本。
- 预置模板目前是种子数据，缺少真实模板代码包和前端筛选/详情闭环。

### 版本控制完整能力

- 已有手动快照、列表和回滚，但每次 AI 调整自动快照尚未接入。
- 缺少 Diff 视图、分支管理、版本对比、部署版本标记和版本清理策略。
- 快照依赖 MinIO，缺少对象不存在、下载失败、回滚失败时的一致性保护。

### AI 智能路由与协作

- 编程工具失败切换已有基础逻辑，但任务分类模型尚未实现。
- UI/API/性能任务自动选择编程工具的策略尚未实现。
- 对话式开发的澄清问答、需求确认和持续上下文管理还不完整。
- 知识库、历史成功案例、企业编码规范自动注入尚未完成。
- `MultiAgentService` 未接入 API 或前端，Multi-Agent 并行协作尚未形成用户可用闭环。
- AI Code Review、自动修复 ESLint/Pylint 错误尚未实现。

### 测试系统升级

- 仍以 Playwright 冒烟测试和后端 pytest 为主。
- 自动生成 pytest/Jest 单元测试尚未实现。
- API 集成测试和 Postman Collections 尚未实现。
- Lighthouse CI 性能测试尚未实现。
- OWASP ZAP 安全扫描尚未实现。
- 测试报告趋势、覆盖率可视化和失败自动回滚尚未实现。
- 每次代码变更自动触发测试尚未实现。

### 部署流程优化

- 多环境模型 `Development/Staging/Production` 尚未实现。
- 每环境独立环境变量和密钥管理尚未实现。
- Vault 集成尚未实现。
- GitHub Actions / GitLab CI 集成尚未实现。
- 蓝绿部署、金丝雀发布、灰度流量、健康检查失败自动回滚尚未实现。
- K8s 生产部署目前只有基础清单，尚未形成完整发布流水线。

### 监控与告警

- Prometheus/Grafana 已有基础，但 P50/P95/P99、QPS、错误率、资源指标、业务指标和成本分析尚未完整采集。
- `task_queue_length` 指标已定义但未看到真实队列长度更新。
- 告警规则、Alertmanager、邮件/短信/钉钉通知、告警聚合尚未实现。
- 自动扩容触发规则尚未实现。
- APM、OpenTelemetry、Jaeger 尚未实现。

### 安全要求

- TLS 1.3 未在应用或部署清单中闭环。
- OAuth 2.0 / OIDC 登录未实现。
- API 限流 `100 req/min/user` 未实现。
- CORS 配置已存在，但生产白名单策略需要固化。
- 审计日志、不可篡改日志、S3 Object Lock 未实现。
- Dependabot、Trivy、定期渗透测试流程未实现。
- SSRF 防护已在模型服务接口出现，但需要扩展到所有可发起外部请求的位置。

### 高可用、备份与恢复

- K8s 主服务和 Worker 清单当前为 1 副本，未达到 3 实例高可用目标。
- PostgreSQL 主从复制和自动故障转移未实现。
- Redis Cluster、PostgreSQL 分片、跨可用区部署未实现。
- 数据库每日全量备份、每小时增量备份、站点代码实时同步、RTO/RPO 目标尚未实现。
- AI 编程工具故障降级为模板生成、熔断、指数退避和抖动策略尚未实现。

### 成本控制

- 组织表已有 `ai_quota_monthly` 字段，但 AI 调用额度扣减、超限拦截、套餐规则尚未实现。
- Token 调用前预估、成本提示、相似 Prompt 缓存复用尚未实现。
- 站点 24 小时无访问自动休眠和首次访问唤醒尚未实现。

### GraphQL、多云与服务网格

- GraphQL API 未实现。
- AWS ECS、阿里云 ACK、腾讯云 TKE、自建 K8s 的抽象部署接口未实现。
- Istio 或其他服务网格未实现。
