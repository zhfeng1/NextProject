[pytest]
testpaths = backend/tests
python_files = test_*.py
asyncio_mode = auto
addopts =
    --strict-markers
    --cov=backend
    --cov-report=term-missing
markers =
    unit: Unit tests
    integration: Integration tests
