---
phase: 2
plan: 2
title: "仓库创建与导入 — ProjectService + API + Celery 异步克隆"
wave: 2
depends_on:
  - PLAN-01-data-model-migration
files_modified:
  - backend/services/project_service.py
  - backend/services/site_service.py
  - backend/services/task_service.py
  - backend/tasks/clone_repo.py
  - backend/api/v2/projects.py
  - backend/api/__init__.py
  - backend/tests/test_projects.py
autonomous: true
requirements:
  - PROJ-01
  - PROJ-02
  - PROJ-03
  - PROJ-04
  - PROJ-05
---

# Plan 02: 仓库创建与导入 — ProjectService + API + Celery 异步克隆

## Goal

实现 Project CRUD 服务层 + REST API 端点、空白仓库创建、git URL 异步克隆（Celery 任务）、仓库文件浏览 API。项目下多仓库按 `generated_sites/<project_id>/<repo_name>/` 存储。

<threat_model>

## Threat Model

| Threat | Severity | Mitigation |
|--------|----------|------------|
| T-02-01: 文件浏览路径穿越 | High | `repo_file_path` API 使用 `Path.resolve()` 后检查是否在仓库根目录内，拒绝 `../` 穿越 |
| T-02-04: git clone 命令注入 | High | git_url 通过 `shlex.quote()` 转义后传入 subprocess；不拼接用户输入到 shell 命令 |
| T-02-05: 未授权访问他人项目 | Medium | 所有 API 端点通过 `owner_id/org_id` 权限检查，复用现有 `get_current_user` 依赖 |
| T-02-06: git_password 明文泄露 | Medium | git_password 写入 AgentTask.payload 前加密，读取时解密（复用 Phase 1 加密工具） |
| T-02-09: 文件浏览端点仓库归属绕过 | Medium | `list_repo_files` / `get_repo_file` 校验 `site.project_id == project_id`，防止通过构造 URL 访问其他项目的仓库 |

**Blocking threats:** T-02-01 和 T-02-04 通过输入验证和参数化缓解。

</threat_model>

## Tasks

<task id="02-02-01">
<title>创建 ProjectService 服务层</title>
<read_first>
- backend/services/site_service.py (行 122-124 site_root 方法, 行 398-425 serialize/list 模式, 行 460-518 create_site 模式)
- backend/models/project.py (Project 模型字段)
- backend/models/site.py (Site 模型 + project_id FK)
- backend/models/enums.py (SiteStatus 枚举)
- backend/core/encryption.py (加密工具，用于 git_password 加密)
</read_first>
<action>
新建 `backend/services/project_service.py`：

```python
from __future__ import annotations

import re
import uuid
from pathlib import Path
from typing import Any

from fastapi import HTTPException
from sqlalchemy import or_, select
from sqlalchemy.ext.asyncio import AsyncSession

from backend.models.project import Project
from backend.models.site import Site, SiteStatus

GENERATED_SITES_ROOT = Path(
    __import__("os").environ.get("GENERATED_SITES_ROOT", "generated_sites")
)

# [ISSUE-04 修复] 仓库名称合法性校验：仅允许字母数字、连字符、下划线、点
REPO_NAME_PATTERN = re.compile(r"^[a-zA-Z0-9][a-zA-Z0-9._-]*$")
MAX_REPO_NAME_LENGTH = 128


def validate_repo_name(name: str) -> str:
    """校验并返回清洗后的仓库名称。"""
    name = name.strip()
    if not name or len(name) > MAX_REPO_NAME_LENGTH:
        raise HTTPException(status_code=400, detail="Repo name must be 1-128 characters")
    if not REPO_NAME_PATTERN.match(name):
        raise HTTPException(
            status_code=400,
            detail="Repo name may only contain letters, digits, hyphens, underscores, and dots, and must start with a letter or digit",
        )
    return name


class ProjectService:
    # ---- 路径 ----

    def project_root(self, project_id: str) -> Path:
        return GENERATED_SITES_ROOT / project_id

    def repo_root(self, project_id: str, repo_name: str) -> Path:
        return self.project_root(project_id) / repo_name

    # ---- 序列化 ----

    def serialize_project(self, project: Project, repos: list[Site] | None = None) -> dict[str, Any]:
        result = {
            "id": str(project.id),
            "name": project.name,
            "description": project.description or "",
            "repo_count": len(repos) if repos is not None else 0,
            "created_at": project.created_at.isoformat() if getattr(project, "created_at", None) else None,
            "updated_at": project.updated_at.isoformat() if getattr(project, "updated_at", None) else None,
        }
        if repos is not None:
            from backend.services.site_service import site_service
            result["repos"] = [site_service.serialize_site(r) for r in repos]
        return result

    # ---- CRUD ----

    async def list_projects(self, db: AsyncSession, user: object, include_deleted: bool = False) -> list[Project]:
        query = select(Project)
        user_id = getattr(user, "id", None)
        org_id = getattr(user, "default_org_id", None)
        if user_id is not None:
            query = query.where(or_(Project.owner_id == user_id, Project.org_id == org_id))
        if not include_deleted:
            query = query.where(Project.deleted_at.is_(None))
        query = query.order_by(Project.created_at.asc())
        rows = await db.execute(query)
        return list(rows.scalars().all())

    async def get_project(self, db: AsyncSession, project_id: str, current_user: object) -> Project:
        project = await db.get(Project, project_id)
        if project is None or project.deleted_at is not None:
            raise HTTPException(status_code=404, detail="Project not found")
        user_id = getattr(current_user, "id", None)
        org_id = getattr(current_user, "default_org_id", None)
        if str(project.owner_id) != str(user_id) and str(project.org_id) != str(org_id):
            raise HTTPException(status_code=404, detail="Project not found")
        return project

    async def get_project_repos(self, db: AsyncSession, project_id: str) -> list[Site]:
        query = select(Site).where(Site.project_id == project_id, Site.deleted_at.is_(None))
        rows = await db.execute(query)
        return list(rows.scalars().all())

    async def create_project(
        self,
        db: AsyncSession,
        current_user: object,
        name: str,
        description: str = "",
    ) -> Project:
        project = Project(
            id=str(uuid.uuid4()),
            name=name.strip(),
            description=(description or "").strip(),
            owner_id=getattr(current_user, "id", None),
            org_id=getattr(current_user, "default_org_id", None),
        )
        db.add(project)
        await db.commit()
        await db.refresh(project)
        return project

    async def update_project(
        self,
        db: AsyncSession,
        project_id: str,
        current_user: object,
        name: str | None = None,
        description: str | None = None,
    ) -> Project:
        project = await self.get_project(db, project_id, current_user)
        if name is not None:
            project.name = name.strip()
        if description is not None:
            project.description = description.strip()
        await db.commit()
        await db.refresh(project)
        return project

    async def delete_project(self, db: AsyncSession, project_id: str, current_user: object) -> None:
        project = await self.get_project(db, project_id, current_user)
        from datetime import datetime, timezone
        project.deleted_at = datetime.now(timezone.utc)
        # 软删除项目下所有仓库
        repos = await self.get_project_repos(db, project_id)
        for repo in repos:
            repo.deleted_at = datetime.now(timezone.utc)
        await db.commit()

    async def add_repo(
        self,
        db: AsyncSession,
        project_id: str,
        current_user: object,
        repo_name: str,
        git_url: str | None = None,
        git_branch: str | None = None,
        git_username: str | None = None,
        git_password: str | None = None,
    ) -> Site:
        """添加仓库到项目（空白创建或 git 克隆）"""
        from backend.services.site_service import site_service

        project = await self.get_project(db, project_id, current_user)

        # [ISSUE-04 修复] 校验仓库名称合法性
        repo_name = validate_repo_name(repo_name)

        site_id = str(uuid.uuid4())
        config: dict[str, Any] = {}
        if git_url:
            config["git_source"] = {
                "url": git_url,
                "username": git_username or "",
                "branch": git_branch or "",
            }

        site = Site(
            id=str(uuid.uuid4()),
            site_id=site_id,
            name=repo_name,
            owner_id=str(project.owner_id),
            org_id=str(project.org_id),
            project_id=project_id,
            status=SiteStatus.BUILDING.value if git_url else SiteStatus.STOPPED.value,
            config=config,
        )
        db.add(site)
        await db.flush()

        if git_url:
            # [ISSUE-03 修复] git_password 加密后再存入 payload
            encrypted_password = ""
            if git_password:
                from backend.core.encryption import encrypt_value
                encrypted_password = encrypt_value(git_password)

            # 异步克隆：创建 AgentTask，Celery 执行
            from backend.models.task import AgentTask
            from backend.models.enums import TaskType, TaskStatus
            task = AgentTask(
                id=str(uuid.uuid4()),
                site_id=site.id,
                task_type=TaskType.CLONE_REPO.value,
                status=TaskStatus.QUEUED.value,
                payload={
                    "git_url": git_url,
                    "git_branch": git_branch or "",
                    "git_username": git_username or "",
                    "git_password_encrypted": encrypted_password,
                    "project_id": project_id,
                    "repo_name": repo_name,
                },
            )
            db.add(task)
            await db.flush()
            await db.commit()
            from backend.services.task_service import task_service
            task_service.enqueue_task(task)
        else:
            # 空白仓库：直接在 project 分组路径下初始化
            repo_path = self.repo_root(project_id, repo_name)
            site_service.ensure_site_structure(site.site_id, override_root=repo_path)
            await db.commit()

        await db.refresh(site)
        return site


project_service = ProjectService()
```

关键点：
- 单例模式 `project_service = ProjectService()`
- 路径分组：`generated_sites/<project_id>/<repo_name>/`
- 权限检查：`owner_id` 或 `org_id` 匹配
- 空白仓库调用 `site_service.ensure_site_structure()` 传 `override_root` 参数
- git 克隆走 Celery 异步任务
- **[ISSUE-03 修复]** `git_password` 在写入 `AgentTask.payload` 前通过 `encrypt_value()` 加密，payload 字段名改为 `git_password_encrypted`
- **[ISSUE-04 修复]** `repo_name` 通过 `validate_repo_name()` 做严格校验，仅允许字母数字、连字符、下划线、点，防止路径注入
</action>
<acceptance_criteria>
- `backend/services/project_service.py` 文件存在
- 文件包含 `class ProjectService:`
- 文件包含 `project_service = ProjectService()`
- 文件包含 `async def list_projects(`
- 文件包含 `async def create_project(`
- 文件包含 `async def add_repo(`
- 文件包含 `def repo_root(self, project_id: str, repo_name: str) -> Path:`
- 文件包含 `GENERATED_SITES_ROOT`
- 文件包含 `validate_repo_name`
- 文件包含 `REPO_NAME_PATTERN`
- 文件包含 `encrypt_value`
- 文件包含 `git_password_encrypted`
</acceptance_criteria>
</task>

<task id="02-02-02">
<title>修改 SiteService 支持 override_root 参数</title>
<read_first>
- backend/services/site_service.py (行 122-149 site_root/ensure_site_structure 方法)
</read_first>
<action>
修改 `backend/services/site_service.py` 的 `ensure_site_structure` 方法签名，添加可选参数 `override_root`:

在当前方法（大约行 155-185 附近，具体行号需读取确认）的签名中：
```python
def ensure_site_structure(self, site_id: str, override_root: Path | None = None) -> Path:
```

方法体开头修改为：
```python
    root = override_root if override_root is not None else self.site_root(site_id)
```

其余逻辑不变（创建 backend/frontend/docs 目录 + git init）。这样 ProjectService 可传入 `generated_sites/<project_id>/<repo_name>/` 作为 root。
</action>
<acceptance_criteria>
- `backend/services/site_service.py` 包含 `def ensure_site_structure(self, site_id: str, override_root`
- `backend/services/site_service.py` 包含 `override_root if override_root is not None else self.site_root(site_id)`
- `cd backend && python -m pytest tests/test_sites.py -v` 全部 PASS（不破坏现有行为）
</acceptance_criteria>
</task>

<task id="02-02-03">
<title>创建 clone_repo Celery 任务</title>
<read_first>
- backend/tasks/develop_code.py (完整文件, Celery 任务模式)
- backend/tasks/_helpers.py (task_db_session 工具)
- backend/core/redis_lock.py (acquire_site_lock/release_site_lock)
- backend/services/site_service.py (clone_site_repository 方法签名)
- backend/core/encryption.py (decrypt_value, 用于解密 git_password)
</read_first>
<action>
新建 `backend/tasks/clone_repo.py`：

```python
from __future__ import annotations

import asyncio

from backend.core.celery_app import celery_app
from backend.core.redis_lock import acquire_site_lock, release_site_lock
from backend.models import AgentTask
from backend.models.enums import TaskStatus, SiteStatus
from backend.models.site import Site
from backend.tasks._helpers import task_db_session


@celery_app.task(bind=True, max_retries=60, default_retry_delay=30)
def clone_repo_task(self, task_id: str) -> dict[str, object]:
    async def _run() -> dict[str, object]:
        async with task_db_session() as db:
            task = await db.get(AgentTask, task_id)
            if task is None:
                raise ValueError(f"Task not found: {task_id}")
            site_id = str(task.site_id)
            payload = task.payload or {}

        if not acquire_site_lock(site_id, task_id):
            raise self.retry(countdown=30)

        try:
            async with task_db_session() as db:
                task = await db.get(AgentTask, task_id)
                site = await db.get(Site, str(task.site_id))
                if site is None:
                    raise ValueError(f"Site not found: {task.site_id}")

                payload = task.payload or {}
                git_url = payload.get("git_url", "")
                git_branch = payload.get("git_branch", "")
                git_username = payload.get("git_username", "")
                project_id = payload.get("project_id", "")
                repo_name = payload.get("repo_name", site.name)

                # [ISSUE-03 修复] 解密 git_password
                git_password = ""
                encrypted_pw = payload.get("git_password_encrypted", "")
                if encrypted_pw:
                    from backend.core.encryption import decrypt_value
                    git_password = decrypt_value(encrypted_pw)

                from backend.services.site_service import site_service
                from backend.services.project_service import project_service

                clone_root = project_service.repo_root(project_id, repo_name) if project_id else None

                try:
                    site_service.clone_site_repository(
                        site.site_id,
                        git_url,
                        git_username=git_username,
                        git_password=git_password,
                        git_branch=git_branch,
                        override_root=clone_root,
                    )
                    site.status = SiteStatus.STOPPED.value
                    task.status = TaskStatus.SUCCESS.value
                except Exception as exc:
                    site.status = SiteStatus.ERROR.value
                    task.status = TaskStatus.FAILED.value
                    task.payload = {**(task.payload or {}), "error": str(exc)}
                    raise
                finally:
                    await db.commit()

                from backend.services.task_service import task_service
                return task_service.serialize_task(task)
        finally:
            release_site_lock(site_id, task_id)

    return asyncio.run(_run())
```

关键点：
- 与 `develop_code_task` 完全相同的模式：`@celery_app.task(bind=True, max_retries=60, default_retry_delay=30)` + `asyncio.run()`
- **[ISSUE-02 修复]** 使用 `from backend.models import AgentTask`（正确的类名），而非 `Task`
- **[ISSUE-03 修复]** 从 payload 中读取 `git_password_encrypted` 并通过 `decrypt_value()` 解密
- 获取 Redis 锁（site 级别），防止并发
- 成功时 `site.status = "stopped"`, `task.status = "success"`
- 失败时 `site.status = "error"`, `task.status = "failed"`, 错误信息写入 `task.payload`
</action>
<acceptance_criteria>
- `backend/tasks/clone_repo.py` 文件存在
- 文件包含 `@celery_app.task(bind=True, max_retries=60, default_retry_delay=30)`
- 文件包含 `def clone_repo_task(self, task_id: str)`
- 文件包含 `from backend.models import AgentTask`
- 文件**不**包含 `from backend.models import Task`（确认 ISSUE-02 已修复）
- 文件包含 `acquire_site_lock(site_id, task_id)`
- 文件包含 `release_site_lock(site_id, task_id)`
- 文件包含 `site.status = SiteStatus.STOPPED.value`
- 文件包含 `site.status = SiteStatus.ERROR.value`
- 文件包含 `decrypt_value`
- 文件包含 `git_password_encrypted`
</acceptance_criteria>
</task>

<task id="02-02-04">
<title>修改 SiteService.clone_site_repository 支持 override_root</title>
<read_first>
- backend/services/site_service.py (clone_site_repository 方法, 完整签名和逻辑)
</read_first>
<action>
修改 `backend/services/site_service.py` 的 `clone_site_repository` 方法签名，添加可选参数 `override_root: Path | None = None`。

方法体开头将目标目录改为：
```python
target = override_root if override_root is not None else self.site_root(site_id)
```

其余 git clone 逻辑不变，只是工作目录改为 `target`。
</action>
<acceptance_criteria>
- `backend/services/site_service.py` 包含 `clone_site_repository` 方法签名中含 `override_root`
- `cd backend && python -m pytest tests/test_sites.py::test_create_site_from_git_repository -v` PASS
</acceptance_criteria>
</task>

<task id="02-02-05">
<title>TaskService 注册 clone_repo 任务入队</title>
<read_first>
- backend/services/task_service.py (行 31, SUPPORTED_TASK_TYPES 集合; 行 315-330, enqueue_task 方法)
</read_first>
<action>
修改 `backend/services/task_service.py`：

1. 在 `SUPPORTED_TASK_TYPES` 集合中添加 `"clone_repo"`：
   ```python
   SUPPORTED_TASK_TYPES = {"develop_code", "test_local_playwright", "deploy_local", "deploy_apollo", "clone_repo"}
   ```

2. 在 `enqueue_task` 方法中，`elif task.task_type == "test_local_playwright":` 分支之后添加：
   ```python
   elif task.task_type == "clone_repo":
       from backend.tasks.clone_repo import clone_repo_task
       clone_repo_task.delay(str(task.id))
   ```
</action>
<acceptance_criteria>
- `grep "clone_repo" backend/services/task_service.py` 至少有 3 行输出（SUPPORTED_TASK_TYPES + enqueue 2行）
- `backend/services/task_service.py` 包含 `from backend.tasks.clone_repo import clone_repo_task`
- `backend/services/task_service.py` 包含 `clone_repo_task.delay(str(task.id))`
</acceptance_criteria>
</task>

<task id="02-02-06">
<title>创建 Projects API 路由</title>
<read_first>
- backend/api/v2/sites.py (完整文件, 路由声明、依赖注入、响应格式参考)
- backend/api/deps.py (get_current_user, get_db, require_role 依赖)
- backend/services/project_service.py (新建的 ProjectService, 方法签名)
- backend/services/site_service.py (serialize_site, list_site_files, read_site_file 方法签名)
</read_first>
<action>
新建 `backend/api/v2/projects.py`：

```python
from __future__ import annotations

from typing import Any

from fastapi import APIRouter, Body, Depends, HTTPException, Query
from sqlalchemy.ext.asyncio import AsyncSession

from backend.api.deps import get_current_user, get_db
from backend.services.project_service import project_service
from backend.services.site_service import site_service

router = APIRouter(prefix="/projects")


@router.get("")
async def list_projects(
    current_user: object = Depends(get_current_user),
    db: AsyncSession = Depends(get_db),
) -> dict[str, Any]:
    projects = await project_service.list_projects(db, user=current_user)
    result = []
    for p in projects:
        repos = await project_service.get_project_repos(db, str(p.id))
        result.append(project_service.serialize_project(p, repos))
    return {"ok": True, "projects": result}


@router.post("")
async def create_project(
    payload: dict[str, Any] = Body(default_factory=dict),
    current_user: object = Depends(get_current_user),
    db: AsyncSession = Depends(get_db),
) -> dict[str, Any]:
    name = (payload.get("name") or "").strip()
    if not name:
        return {"ok": False, "error": "name is required"}
    description = (payload.get("description") or "").strip()
    project = await project_service.create_project(db, current_user, name, description)
    return {"ok": True, "project": project_service.serialize_project(project, [])}


@router.get("/{project_id}")
async def get_project(
    project_id: str,
    current_user: object = Depends(get_current_user),
    db: AsyncSession = Depends(get_db),
) -> dict[str, Any]:
    project = await project_service.get_project(db, project_id, current_user)
    repos = await project_service.get_project_repos(db, project_id)
    return {"ok": True, "project": project_service.serialize_project(project, repos)}


@router.put("/{project_id}")
async def update_project(
    project_id: str,
    payload: dict[str, Any] = Body(default_factory=dict),
    current_user: object = Depends(get_current_user),
    db: AsyncSession = Depends(get_db),
) -> dict[str, Any]:
    project = await project_service.update_project(
        db, project_id, current_user,
        name=payload.get("name"),
        description=payload.get("description"),
    )
    repos = await project_service.get_project_repos(db, project_id)
    return {"ok": True, "project": project_service.serialize_project(project, repos)}


@router.delete("/{project_id}")
async def delete_project(
    project_id: str,
    current_user: object = Depends(get_current_user),
    db: AsyncSession = Depends(get_db),
) -> dict[str, Any]:
    await project_service.delete_project(db, project_id, current_user)
    return {"ok": True}


@router.post("/{project_id}/repos")
async def add_repo(
    project_id: str,
    payload: dict[str, Any] = Body(default_factory=dict),
    current_user: object = Depends(get_current_user),
    db: AsyncSession = Depends(get_db),
) -> dict[str, Any]:
    repo_name = (payload.get("name") or "").strip()
    if not repo_name:
        return {"ok": False, "error": "name is required"}
    site = await project_service.add_repo(
        db, project_id, current_user,
        repo_name=repo_name,
        git_url=(payload.get("git_url") or "").strip() or None,
        git_branch=(payload.get("git_branch") or "").strip() or None,
        git_username=(payload.get("git_username") or "").strip() or None,
        git_password=(payload.get("git_password") or "").strip() or None,
    )
    return {"ok": True, "repo": site_service.serialize_site(site)}


@router.get("/{project_id}/repos/{repo_id}/files")
async def list_repo_files(
    project_id: str,
    repo_id: str,
    path: str = Query(default=""),
    current_user: object = Depends(get_current_user),
    db: AsyncSession = Depends(get_db),
) -> dict[str, Any]:
    project = await project_service.get_project(db, project_id, current_user)
    site = await site_service.get_site_by_public_id(db, repo_id, current_user)
    # [NEW-03 修复] 校验仓库归属：site 必须属于当前 project，防止越权访问
    if str(site.project_id) != str(project_id):
        raise HTTPException(status_code=404, detail="Repo not found in this project")
    # [ISSUE-04 修复] 使用 site.name（已通过 validate_repo_name 校验）作为路径组件
    repo_path = project_service.repo_root(project_id, site.name)
    data = site_service.list_site_files(site.site_id, path, override_root=repo_path)
    return {"ok": True, **data}


@router.get("/{project_id}/repos/{repo_id}/file")
async def get_repo_file(
    project_id: str,
    repo_id: str,
    path: str = Query(default=""),
    current_user: object = Depends(get_current_user),
    db: AsyncSession = Depends(get_db),
) -> dict[str, Any]:
    project = await project_service.get_project(db, project_id, current_user)
    site = await site_service.get_site_by_public_id(db, repo_id, current_user)
    # [NEW-03 修复] 校验仓库归属：site 必须属于当前 project，防止越权访问
    if str(site.project_id) != str(project_id):
        raise HTTPException(status_code=404, detail="Repo not found in this project")
    repo_path = project_service.repo_root(project_id, site.name)
    data = site_service.read_site_file(site.site_id, path, override_root=repo_path)
    return {"ok": True, **data}
```

关键点：
- `router = APIRouter(prefix="/projects")`
- 所有端点使用 `{"ok": True/False}` 响应格式
- 文件浏览端点传 `override_root` 支持 project 分组路径
- 权限检查通过 `project_service.get_project()` 内部实现
- **[NEW-03 修复]** `list_repo_files` 和 `get_repo_file` 在获取 site 后校验 `site.project_id == project_id`，若不匹配返回 404。防止同 org 用户通过构造 `/projects/<自己的project_id>/repos/<他人的repo_id>/files` 访问他人仓库
- **[ISSUE-04]** `site.name` 在 `add_repo` 时已通过 `validate_repo_name()` 校验合法性，确保路径安全
</action>
<acceptance_criteria>
- `backend/api/v2/projects.py` 文件存在
- 文件包含 `router = APIRouter(prefix="/projects")`
- 文件包含 `async def list_projects(`
- 文件包含 `async def create_project(`
- 文件包含 `async def add_repo(`
- 文件包含 `async def list_repo_files(`
- 文件包含 `async def get_repo_file(`
- 文件包含 `"ok": True`
- 文件包含 `site.project_id` (归属校验)
- 文件包含 `Repo not found in this project` (归属校验错误信息)
</acceptance_criteria>
</task>

<task id="02-02-07">
<title>注册 Projects 路由到 API Router</title>
<read_first>
- backend/api/__init__.py (完整文件, 路由注册模式)
</read_first>
<action>
修改 `backend/api/__init__.py`：

1. 在 import 行（第 5 行）的 `workflows` 后添加 `, projects`：
   ```python
   from backend.api.v2 import auth, conversations, deploy, mcp, projects, providers, sites, skills, stats, tasks, templates, versions, websocket, workflows
   ```

2. 在 `router.include_router(sites.router, ...)` 行之前添加：
   ```python
   router.include_router(projects.router, prefix="/api/v2", tags=["Projects"])
   ```
</action>
<acceptance_criteria>
- `backend/api/__init__.py` 包含 `projects` 在 import 行中
- `backend/api/__init__.py` 包含 `router.include_router(projects.router, prefix="/api/v2", tags=["Projects"])`
</acceptance_criteria>
</task>

<task id="02-02-08">
<title>修改 SiteService 文件浏览方法支持 override_root</title>
<read_first>
- backend/services/site_service.py (list_site_files 和 read_site_file 方法, 完整签名和路径逻辑)
</read_first>
<action>
修改 `backend/services/site_service.py` 的 `list_site_files` 和 `read_site_file` 方法，添加可选参数 `override_root: Path | None = None`。

在每个方法体开头，将 root 路径改为：
```python
root = override_root if override_root is not None else self.site_root(site_id)
```

其余逻辑（路径安全检查、文件读取）不变。确保路径穿越检查（`resolve()` 后在 root 内）仍然生效。
</action>
<acceptance_criteria>
- `backend/services/site_service.py` 中 `list_site_files` 方法签名包含 `override_root`
- `backend/services/site_service.py` 中 `read_site_file` 方法签名包含 `override_root`
- `cd backend && python -m pytest tests/test_sites.py::test_site_file_browser_lists_and_reads_files -v` PASS
- `cd backend && python -m pytest tests/test_sites.py::test_site_file_browser_blocks_path_escape -v` PASS
</acceptance_criteria>
</task>

<task id="02-02-09">
<title>扩展 test_projects.py — API 集成测试 + 路径穿越安全测试 + 越权访问测试</title>
<read_first>
- backend/tests/test_projects.py (Plan 01 创建的初始测试文件)
- backend/tests/test_sites.py (API 测试模式, 尤其 git 克隆测试和路径穿越测试)
- backend/api/v2/projects.py (新建的 API 端点)
</read_first>
<action>
在 `backend/tests/test_projects.py` 中追加以下测试：

```python
@pytest.mark.asyncio
async def test_get_project(client: httpx.AsyncClient, auth_headers: dict[str, str]) -> None:
    create = await client.post(
        "/api/v2/projects",
        json={"name": "Get Project"},
        headers=auth_headers,
    )
    project_id = create.json()["project"]["id"]
    response = await client.get(f"/api/v2/projects/{project_id}", headers=auth_headers)
    assert response.status_code == 200
    assert response.json()["project"]["name"] == "Get Project"


@pytest.mark.asyncio
async def test_update_project(client: httpx.AsyncClient, auth_headers: dict[str, str]) -> None:
    create = await client.post(
        "/api/v2/projects",
        json={"name": "Old Name"},
        headers=auth_headers,
    )
    project_id = create.json()["project"]["id"]
    response = await client.put(
        f"/api/v2/projects/{project_id}",
        json={"name": "New Name", "description": "Updated"},
        headers=auth_headers,
    )
    assert response.status_code == 200
    assert response.json()["project"]["name"] == "New Name"
    assert response.json()["project"]["description"] == "Updated"


@pytest.mark.asyncio
async def test_delete_project(client: httpx.AsyncClient, auth_headers: dict[str, str]) -> None:
    create = await client.post(
        "/api/v2/projects",
        json={"name": "Delete Me"},
        headers=auth_headers,
    )
    project_id = create.json()["project"]["id"]
    del_resp = await client.delete(f"/api/v2/projects/{project_id}", headers=auth_headers)
    assert del_resp.status_code == 200
    assert del_resp.json()["ok"] is True
    # 删除后列表不再包含
    list_resp = await client.get("/api/v2/projects", headers=auth_headers)
    assert not any(p["id"] == project_id for p in list_resp.json()["projects"])


@pytest.mark.asyncio
async def test_add_blank_repo(client: httpx.AsyncClient, auth_headers: dict[str, str], app_module) -> None:
    """PROJ-03: 空白仓库创建"""
    create = await client.post(
        "/api/v2/projects",
        json={"name": "Repo Project"},
        headers=auth_headers,
    )
    project_id = create.json()["project"]["id"]
    response = await client.post(
        f"/api/v2/projects/{project_id}/repos",
        json={"name": "frontend"},
        headers=auth_headers,
    )
    assert response.status_code == 200
    assert response.json()["ok"] is True
    assert response.json()["repo"]["name"] == "frontend"


@pytest.mark.asyncio
async def test_add_repo_invalid_name_rejected(client: httpx.AsyncClient, auth_headers: dict[str, str]) -> None:
    """[ISSUE-04] 仓库名称含非法字符被拒绝"""
    create = await client.post(
        "/api/v2/projects",
        json={"name": "Name Validation Project"},
        headers=auth_headers,
    )
    project_id = create.json()["project"]["id"]
    # 包含路径分隔符的名称应被拒绝
    response = await client.post(
        f"/api/v2/projects/{project_id}/repos",
        json={"name": "../evil-repo"},
        headers=auth_headers,
    )
    assert response.status_code == 400


@pytest.mark.asyncio
async def test_add_repo_slash_in_name_rejected(client: httpx.AsyncClient, auth_headers: dict[str, str]) -> None:
    """[ISSUE-04] 仓库名称含斜杠被拒绝"""
    create = await client.post(
        "/api/v2/projects",
        json={"name": "Slash Validation Project"},
        headers=auth_headers,
    )
    project_id = create.json()["project"]["id"]
    response = await client.post(
        f"/api/v2/projects/{project_id}/repos",
        json={"name": "foo/bar"},
        headers=auth_headers,
    )
    assert response.status_code == 400


@pytest.mark.asyncio
async def test_repo_file_path_escape_blocked(client: httpx.AsyncClient, auth_headers: dict[str, str], app_module) -> None:
    """[ISSUE-07] 路径穿越防护：../不能逃逸出仓库根目录"""
    # 创建项目和空白仓库
    create = await client.post(
        "/api/v2/projects",
        json={"name": "Path Escape Project"},
        headers=auth_headers,
    )
    project_id = create.json()["project"]["id"]
    repo_resp = await client.post(
        f"/api/v2/projects/{project_id}/repos",
        json={"name": "test-repo"},
        headers=auth_headers,
    )
    repo_id = repo_resp.json()["repo"]["site_id"]

    # 尝试路径穿越读取文件
    escape_resp = await client.get(
        f"/api/v2/projects/{project_id}/repos/{repo_id}/file",
        params={"path": "../../etc/passwd"},
        headers=auth_headers,
    )
    # 应返回 400 或 403，不能返回 200
    assert escape_resp.status_code in (400, 403, 404)

    # 尝试路径穿越列出目录
    escape_list_resp = await client.get(
        f"/api/v2/projects/{project_id}/repos/{repo_id}/files",
        params={"path": "../../../"},
        headers=auth_headers,
    )
    assert escape_list_resp.status_code in (400, 403, 404)


@pytest.mark.asyncio
async def test_repo_files_cross_project_blocked(
    client: httpx.AsyncClient, auth_headers: dict[str, str], app_module
) -> None:
    """[NEW-03] 越权访问测试：用自己的 project_id 访问另一个项目的 repo 应返回 404"""
    # 创建项目 A 并添加仓库
    create_a = await client.post(
        "/api/v2/projects",
        json={"name": "Project A"},
        headers=auth_headers,
    )
    project_a_id = create_a.json()["project"]["id"]
    repo_a_resp = await client.post(
        f"/api/v2/projects/{project_a_id}/repos",
        json={"name": "repo-a"},
        headers=auth_headers,
    )
    repo_a_id = repo_a_resp.json()["repo"]["site_id"]

    # 创建项目 B（同一用户）
    create_b = await client.post(
        "/api/v2/projects",
        json={"name": "Project B"},
        headers=auth_headers,
    )
    project_b_id = create_b.json()["project"]["id"]

    # 使用项目 B 的 project_id 尝试访问项目 A 的 repo 文件列表 → 应返回 404
    cross_files_resp = await client.get(
        f"/api/v2/projects/{project_b_id}/repos/{repo_a_id}/files",
        headers=auth_headers,
    )
    assert cross_files_resp.status_code == 404

    # 使用项目 B 的 project_id 尝试读取项目 A 的 repo 文件 → 应返回 404
    cross_file_resp = await client.get(
        f"/api/v2/projects/{project_b_id}/repos/{repo_a_id}/file",
        params={"path": "index.html"},
        headers=auth_headers,
    )
    assert cross_file_resp.status_code == 404


@pytest.mark.asyncio
async def test_project_not_found(client: httpx.AsyncClient, auth_headers: dict[str, str]) -> None:
    response = await client.get("/api/v2/projects/nonexistent-id", headers=auth_headers)
    assert response.status_code == 404


@pytest.mark.asyncio
async def test_create_project_requires_name(client: httpx.AsyncClient, auth_headers: dict[str, str]) -> None:
    response = await client.post(
        "/api/v2/projects",
        json={"name": ""},
        headers=auth_headers,
    )
    assert response.status_code == 200
    assert response.json()["ok"] is False
```
</action>
<acceptance_criteria>
- `backend/tests/test_projects.py` 包含 `test_get_project`
- `backend/tests/test_projects.py` 包含 `test_update_project`
- `backend/tests/test_projects.py` 包含 `test_delete_project`
- `backend/tests/test_projects.py` 包含 `test_add_blank_repo`
- `backend/tests/test_projects.py` 包含 `test_add_repo_invalid_name_rejected`
- `backend/tests/test_projects.py` 包含 `test_add_repo_slash_in_name_rejected`
- `backend/tests/test_projects.py` 包含 `test_repo_file_path_escape_blocked`
- `backend/tests/test_projects.py` 包含 `test_repo_files_cross_project_blocked`
- `backend/tests/test_projects.py` 包含 `test_project_not_found`
- `cd backend && python -m pytest tests/test_projects.py -v` 全部 PASS
- `cd backend && python -m pytest tests/test_sites.py -v` 全部 PASS
</acceptance_criteria>
</task>

## Verification

```bash
# 1. 全部项目测试通过
cd backend && python -m pytest tests/test_projects.py -v

# 2. 现有站点测试通过（向后兼容）
cd backend && python -m pytest tests/test_sites.py -v

# 3. 全套测试
cd backend && python -m pytest tests/ -v

# 4. 关键文件内容验证
grep "class ProjectService" backend/services/project_service.py
grep "clone_repo_task" backend/tasks/clone_repo.py
grep "router = APIRouter" backend/api/v2/projects.py
grep "projects" backend/api/__init__.py
grep "clone_repo" backend/services/task_service.py
grep "AgentTask" backend/tasks/clone_repo.py
grep "validate_repo_name" backend/services/project_service.py
grep "encrypt_value\|decrypt_value" backend/services/project_service.py backend/tasks/clone_repo.py
grep "test_repo_file_path_escape_blocked" backend/tests/test_projects.py
grep "test_repo_files_cross_project_blocked" backend/tests/test_projects.py
grep "site.project_id" backend/api/v2/projects.py
```

## must_haves

- [ ] ProjectService 实现 CRUD + add_repo + repo_root 路径
- [ ] Projects API 端点（GET/POST/PUT/DELETE /projects + POST /projects/:id/repos + 文件浏览）
- [ ] clone_repo Celery 任务：异步克隆 + 状态更新 + Redis 锁
- [ ] clone_repo 任务使用 `AgentTask`（非 `Task`）模型类名
- [ ] TaskService 注册 clone_repo 入队
- [ ] SiteService 的 ensure_site_structure / clone_site_repository / 文件浏览支持 override_root
- [ ] 路径穿越防护在 override_root 场景下仍然生效
- [ ] git_password 加密存储（写入前 encrypt_value，读取时 decrypt_value）
- [ ] repo_name 严格校验（仅字母数字、连字符、下划线、点）
- [ ] 文件浏览端点校验 `site.project_id == project_id`（防止越权访问他人仓库）
- [ ] test_repo_file_path_escape_blocked 路径穿越安全测试通过
- [ ] test_repo_files_cross_project_blocked 越权访问测试通过
- [ ] 全部 test_projects.py 和 test_sites.py 测试 PASS
