---
phase: 2
plan: 3
title: "项目管理前端 — 列表、创建、路由、导航"
wave: 3
depends_on:
  - PLAN-02-repo-create-import
files_modified:
  - frontend/src/types/models.ts
  - frontend/src/api/projects.ts
  - frontend/src/stores/project.ts
  - frontend/src/views/Projects/ProjectList.vue
  - frontend/src/views/Projects/ProjectDetail.vue
  - frontend/src/router/index.ts
  - frontend/src/components/Layout/AppLayout.vue
autonomous: true
requirements:
  - PROJ-01
  - PROJ-02
  - PROJ-03
  - PROJ-04
  - PROJ-05
---

# Plan 03: 项目管理前端 — 列表、创建、路由、导航

## Goal

新建项目管理前端页面（ProjectList + ProjectDetail），替换侧边栏"站点管理"为"项目管理"，`/sites` 重定向到 `/projects`，完成前端从 Site 到 Project 概念的迁移。

<threat_model>

## Threat Model

| Threat | Severity | Mitigation |
|--------|----------|------------|
| T-02-06: XSS 通过项目名/描述 | Medium | Vue 模板默认 HTML 转义；不使用 `v-html` 渲染用户输入 |
| T-02-07: 前端路由绕过认证 | Low | `router.beforeEach` 守卫已存在，新路由设置 `requiresAuth: true` |

**No blocking threats.**

</threat_model>

## Tasks

<task id="02-03-01">
<title>添加 Project 和 ProjectRepo TypeScript 类型</title>
<read_first>
- frontend/src/types/models.ts (完整文件, 现有 Site/SiteCreateRequest 接口)
</read_first>
<action>
修改 `frontend/src/types/models.ts`，在 `SiteUpdateRequest` 接口之后添加：

```typescript
export interface Project {
  id: string
  name: string
  description: string
  repo_count: number
  repos?: Site[]
  created_at: string
  updated_at?: string
}

export interface ProjectCreateRequest {
  name: string
  description?: string
}

export interface RepoAddRequest {
  name: string
  git_url?: string
  git_branch?: string
  git_username?: string
  git_password?: string
}
```

同时在 `Site` 接口中添加可选字段：
```typescript
project_id?: string
```
（在 `created_at: string` 之前添加）
</action>
<acceptance_criteria>
- `frontend/src/types/models.ts` 包含 `export interface Project {`
- `frontend/src/types/models.ts` 包含 `export interface ProjectCreateRequest {`
- `frontend/src/types/models.ts` 包含 `export interface RepoAddRequest {`
- `frontend/src/types/models.ts` 的 Site 接口包含 `project_id?: string`
</acceptance_criteria>
</task>

<task id="02-03-02">
<title>创建 projects API 客户端</title>
<read_first>
- frontend/src/api/sites.ts (完整文件, API 客户端模式)
- frontend/src/api/client.ts (axios 实例)
- frontend/src/types/models.ts (Project/RepoAddRequest 类型)
</read_first>
<action>
新建 `frontend/src/api/projects.ts`：

```typescript
import client from './client'
import type { Project, ProjectCreateRequest, RepoAddRequest, Site } from '@/types/models'

export const projectsAPI = {
  list() {
    return client.get<any, { ok: boolean; projects: Project[] }>('/projects')
  },

  create(data: ProjectCreateRequest) {
    return client.post<any, { ok: boolean; project: Project }>('/projects', data)
  },

  get(projectId: string) {
    return client.get<any, { ok: boolean; project: Project }>(`/projects/${projectId}`)
  },

  update(projectId: string, data: Partial<ProjectCreateRequest>) {
    return client.put<any, { ok: boolean; project: Project }>(`/projects/${projectId}`, data)
  },

  delete(projectId: string) {
    return client.delete<any, { ok: boolean }>(`/projects/${projectId}`)
  },

  addRepo(projectId: string, data: RepoAddRequest) {
    return client.post<any, { ok: boolean; repo: Site }>(`/projects/${projectId}/repos`, data)
  },

  listRepoFiles(projectId: string, repoId: string, path = '') {
    return client.get<any, {
      ok: boolean
      current_path: string
      parent_path: string
      entries: Array<{ name: string; path: string; type: 'directory' | 'file'; size?: number | null }>
    }>(`/projects/${projectId}/repos/${repoId}/files`, { params: { path } })
  },

  getRepoFile(projectId: string, repoId: string, path: string) {
    return client.get<any, {
      ok: boolean
      path: string
      name: string
      size: number
      truncated: boolean
      binary: boolean
      content: string
    }>(`/projects/${projectId}/repos/${repoId}/file`, { params: { path } })
  },
}
```
</action>
<acceptance_criteria>
- `frontend/src/api/projects.ts` 文件存在
- 文件包含 `export const projectsAPI`
- 文件包含 `list()`, `create(`, `get(`, `update(`, `delete(`, `addRepo(`
- 文件包含 `listRepoFiles(`, `getRepoFile(`
- 文件包含 `'/projects'`
</acceptance_criteria>
</task>

<task id="02-03-03">
<title>创建 project Pinia store</title>
<read_first>
- frontend/src/stores/site.ts (完整文件, Pinia store 模式)
- frontend/src/api/projects.ts (新建的 API 客户端)
- frontend/src/types/models.ts (Project 类型)
</read_first>
<action>
新建 `frontend/src/stores/project.ts`：

```typescript
import { defineStore } from 'pinia'
import { projectsAPI } from '@/api/projects'
import type { Project, Site } from '@/types/models'

interface ProjectState {
  projects: Project[]
  currentProject: Project | null
  loading: boolean
}

export const useProjectStore = defineStore('project', {
  state: (): ProjectState => ({
    projects: [],
    currentProject: null,
    loading: false,
  }),

  actions: {
    async fetchProjects() {
      this.loading = true
      try {
        const response = await projectsAPI.list()
        this.projects = response.projects
      } finally {
        this.loading = false
      }
    },

    async fetchProject(projectId: string) {
      this.loading = true
      try {
        const response = await projectsAPI.get(projectId)
        this.currentProject = response.project
        return response.project
      } finally {
        this.loading = false
      }
    },

    async createProject(data: { name: string; description?: string }) {
      const response = await projectsAPI.create(data)
      this.projects.unshift(response.project)
      return response.project
    },

    async deleteProject(projectId: string) {
      await projectsAPI.delete(projectId)
      this.projects = this.projects.filter((p) => p.id !== projectId)
    },

    async addRepo(projectId: string, data: { name: string; git_url?: string; git_branch?: string; git_username?: string; git_password?: string }) {
      const response = await projectsAPI.addRepo(projectId, data)
      // 刷新当前项目
      if (this.currentProject?.id === projectId) {
        await this.fetchProject(projectId)
      }
      return response.repo
    },
  },
})
```
</action>
<acceptance_criteria>
- `frontend/src/stores/project.ts` 文件存在
- 文件包含 `export const useProjectStore = defineStore('project',`
- 文件包含 `async fetchProjects()`
- 文件包含 `async fetchProject(`
- 文件包含 `async createProject(`
- 文件包含 `async deleteProject(`
- 文件包含 `async addRepo(`
</acceptance_criteria>
</task>

<task id="02-03-04">
<title>创建 ProjectList.vue 页面</title>
<read_first>
- frontend/src/views/Sites/SiteList.vue (完整文件, 卡片网格布局 + 创建对话框模式)
- frontend/src/stores/project.ts (新建的 project store)
- frontend/src/types/models.ts (Project 类型)
</read_first>
<action>
新建 `frontend/src/views/Projects/ProjectList.vue`（先创建 `frontend/src/views/Projects/` 目录）：

核心要素（基于 SiteList.vue 模式重构）：
- `<script setup lang="ts">` — 使用 `useProjectStore()` + `useRouter()` + `formatDate`
- 导入 shadcn-vue 组件: `Button, Input, Card, CardHeader, CardTitle, CardContent, CardFooter, Dialog, DialogContent, DialogHeader, DialogTitle, DialogFooter, Label`
- lucide-vue-next 图标: `Plus, FolderKanban, Trash2`
- `toast` from `vue-sonner`

State:
```typescript
const filter = ref({ search: '' })
const showCreateDialog = ref(false)
const creating = ref(false)
const createForm = ref({ name: '', description: '' })
```

Computed:
```typescript
const filteredProjects = computed(() => {
  if (!filter.value.search) return projectStore.projects
  const q = filter.value.search.toLowerCase()
  return projectStore.projects.filter(p => p.name.toLowerCase().includes(q))
})
```

Methods:
- `handleCreate()` — 调用 `projectStore.createProject()`，成功后跳转 `/projects/${project.id}`
- `handleDelete(projectId)` — 调用 `projectStore.deleteProject()`
- `onMounted(() => projectStore.fetchProjects())`

Template 结构:
```html
<div class="p-6 space-y-6">
  <!-- 头部: 标题 + 搜索框 + 创建按钮 -->
  <div class="flex items-center justify-between">
    <h1 class="text-2xl font-bold">项目管理</h1>
    <Button @click="showCreateDialog = true"><Plus class="w-4 h-4 mr-2" />新建项目</Button>
  </div>

  <!-- 卡片网格 -->
  <div class="grid gap-4 md:grid-cols-2 lg:grid-cols-3">
    <Card v-for="project in filteredProjects" :key="project.id" class="flex flex-col cursor-pointer" @click="router.push(`/projects/${project.id}`)">
      <CardHeader>
        <CardTitle class="text-lg font-bold flex items-center gap-2">
          <FolderKanban class="w-5 h-5 text-muted-foreground" />
          {{ project.name }}
        </CardTitle>
      </CardHeader>
      <CardContent class="flex-1 text-sm text-muted-foreground space-y-1">
        <p v-if="project.description">{{ project.description }}</p>
        <p>仓库数: {{ project.repo_count }}</p>
        <p>创建于: {{ formatDate(project.created_at) }}</p>
      </CardContent>
      <CardFooter class="justify-end">
        <Button variant="ghost" size="sm" @click.stop="handleDelete(project.id)">
          <Trash2 class="w-4 h-4" />
        </Button>
      </CardFooter>
    </Card>
  </div>

  <!-- 创建对话框 -->
  <Dialog v-model:open="showCreateDialog">
    <DialogContent>
      <DialogHeader><DialogTitle>新建项目</DialogTitle></DialogHeader>
      <div class="space-y-4">
        <div><Label>项目名称</Label><Input v-model="createForm.name" placeholder="输入项目名称" /></div>
        <div><Label>项目描述</Label><Input v-model="createForm.description" placeholder="可选描述" /></div>
      </div>
      <DialogFooter>
        <Button variant="outline" @click="showCreateDialog = false">取消</Button>
        <Button @click="handleCreate" :disabled="creating || !createForm.name.trim()">创建</Button>
      </DialogFooter>
    </DialogContent>
  </Dialog>
</div>
```
</action>
<acceptance_criteria>
- `frontend/src/views/Projects/ProjectList.vue` 文件存在
- 文件包含 `<script setup lang="ts">`
- 文件包含 `useProjectStore`
- 文件包含 `项目管理`
- 文件包含 `新建项目`
- 文件包含 `filteredProjects`
- 文件包含 `handleCreate`
- 文件包含 `FolderKanban`
</acceptance_criteria>
</task>

<task id="02-03-05">
<title>创建 ProjectDetail.vue 页面</title>
<read_first>
- frontend/src/views/Sites/SiteDetail.vue (现有站点详情页, 结构参考)
- frontend/src/views/Projects/ProjectList.vue (新建的项目列表, 组件导入模式)
- frontend/src/stores/project.ts (fetchProject, addRepo 方法)
- frontend/src/types/models.ts (Project, Site 类型)
</read_first>
<action>
新建 `frontend/src/views/Projects/ProjectDetail.vue`：

核心要素：
- `<script setup lang="ts">`
- 通过 `useRoute().params.id` 获取项目 ID
- `onMounted` 调用 `projectStore.fetchProject(projectId)`
- 显示项目名称、描述、仓库列表

Template 结构：
```html
<div class="p-6 space-y-6">
  <!-- 项目信息头 -->
  <div class="flex items-center justify-between">
    <div>
      <h1 class="text-2xl font-bold">{{ project?.name }}</h1>
      <p class="text-muted-foreground">{{ project?.description }}</p>
    </div>
    <div class="flex gap-2">
      <Button @click="showAddRepoDialog = true"><Plus class="w-4 h-4 mr-2" />添加仓库</Button>
      <Button variant="outline" @click="router.push(`/projects/${projectId}/edit`)" v-if="project?.repos?.length">
        <Code class="w-4 h-4 mr-2" />打开编辑器
      </Button>
    </div>
  </div>

  <!-- 仓库列表 -->
  <div class="grid gap-4 md:grid-cols-2 lg:grid-cols-3">
    <Card v-for="repo in project?.repos" :key="repo.site_id" class="flex flex-col">
      <CardHeader>
        <CardTitle class="text-lg flex items-center gap-2">
          <GitBranch class="w-5 h-5" />
          {{ repo.name }}
        </CardTitle>
      </CardHeader>
      <CardContent class="text-sm text-muted-foreground">
        <span :class="repo.status === 'building' ? 'text-yellow-500' : 'text-green-500'">
          {{ repo.status }}
        </span>
      </CardContent>
    </Card>
  </div>

  <!-- 添加仓库对话框 -->
  <Dialog v-model:open="showAddRepoDialog">
    <!-- 仓库名称 + 可选 git URL / branch / username / password -->
  </Dialog>
</div>
```

State:
```typescript
const projectId = useRoute().params.id as string
const showAddRepoDialog = ref(false)
const addRepoForm = ref({ name: '', git_url: '', git_branch: '', git_username: '', git_password: '' })
```

Methods:
- `handleAddRepo()` — 调用 `projectStore.addRepo(projectId, addRepoForm.value)`
- 成功 toast + 清空表单
</action>
<acceptance_criteria>
- `frontend/src/views/Projects/ProjectDetail.vue` 文件存在
- 文件包含 `<script setup lang="ts">`
- 文件包含 `useProjectStore`
- 文件包含 `fetchProject`
- 文件包含 `添加仓库`
- 文件包含 `showAddRepoDialog`
- 文件包含 `handleAddRepo`
</acceptance_criteria>
</task>

<task id="02-03-06">
<title>更新路由配置 — 添加 /projects 路由 + /sites 重定向</title>
<read_first>
- frontend/src/router/index.ts (完整文件, 当前路由结构)
</read_first>
<action>
修改 `frontend/src/router/index.ts`：

1. 在 `children` 数组中，在 `sites` 路由组之前添加 `/projects` 路由组（**不含 ProjectEditor 路由，该路由在 PLAN-04 中随 ProjectEditor.vue 一起注册**）：
   ```typescript
   {
     path: 'projects',
     name: 'ProjectList',
     component: () => import('@/views/Projects/ProjectList.vue'),
   },
   {
     path: 'projects/:id',
     name: 'ProjectDetail',
     component: () => import('@/views/Projects/ProjectDetail.vue'),
   },
   ```

2. 将现有 `sites` 路由的第一个改为重定向：
   ```typescript
   {
     path: 'sites',
     redirect: '/projects',
   },
   ```
   保留 `sites/:id` 和 `sites/:id/edit` 路由不变（SiteEditor 仍在使用）。

**[NEW-02 修复]** `projects/:id/edit` (ProjectEditor) 路由不在此处注册，因为 `ProjectEditor.vue` 在 PLAN-04 才创建。如果在此处注册，Vite 构建时动态 import 不存在的文件会导致 `npm run build` 失败。该路由将在 PLAN-04 Task 02-04-03 中与 `ProjectEditor.vue` 一起注册。
</action>
<acceptance_criteria>
- `frontend/src/router/index.ts` 包含 `path: 'projects'`
- `frontend/src/router/index.ts` 包含 `name: 'ProjectList'`
- `frontend/src/router/index.ts` 包含 `name: 'ProjectDetail'`
- `frontend/src/router/index.ts` **不**包含 `name: 'ProjectEditor'`（该路由在 PLAN-04 注册）
- `frontend/src/router/index.ts` 包含 `redirect: '/projects'` (sites 重定向)
- `frontend/src/router/index.ts` 仍包含 `sites/:id/edit` (兼容保留)
- `cd frontend && npm run build` 编译通过
</acceptance_criteria>
</task>

<task id="02-03-07">
<title>更新侧边栏导航 — "站点管理"改为"项目管理"</title>
<read_first>
- frontend/src/components/Layout/AppLayout.vue (行 67-97, 侧边栏"站点管理"分组)
</read_first>
<action>
修改 `frontend/src/components/Layout/AppLayout.vue`：

1. 行 68：`<SidebarGroupLabel>站点管理</SidebarGroupLabel>` 改为 `<SidebarGroupLabel>项目管理</SidebarGroupLabel>`

2. 行 72：`isActive` 条件从 `route.path === '/sites'` 改为 `route.path === '/projects' || route.path.startsWith('/projects/')`

3. 行 73：`<router-link to="/sites">` 改为 `<router-link to="/projects">`

4. 行 75：`<span>我的站点</span>` 改为 `<span>我的项目</span>`

5. 在 import 中添加 `FolderKanban` 图标，将第一个菜单项的 `Globe` 图标改为 `FolderKanban`（或保持 Globe 也可以，取决于语义偏好。D-08 未指定图标变更，保持 Globe 即可）
</action>
<acceptance_criteria>
- `frontend/src/components/Layout/AppLayout.vue` 包含 `项目管理`
- `frontend/src/components/Layout/AppLayout.vue` 包含 `我的项目`
- `frontend/src/components/Layout/AppLayout.vue` 包含 `to="/projects"`
- `frontend/src/components/Layout/AppLayout.vue` 不再包含 `我的站点`
- `frontend/src/components/Layout/AppLayout.vue` 不再包含 `to="/sites"`（在侧边栏菜单项中）
</acceptance_criteria>
</task>

## Verification

```bash
# 1. 前端编译通过
cd frontend && npm run build

# 2. 后端测试仍通过（无后端变更，但验证不回退）
cd backend && python -m pytest tests/ -v

# 3. 关键内容验证
grep "ProjectList" frontend/src/router/index.ts
grep "项目管理" frontend/src/components/Layout/AppLayout.vue
grep "export const projectsAPI" frontend/src/api/projects.ts
grep "useProjectStore" frontend/src/stores/project.ts
```

## must_haves

- [ ] `Project`, `ProjectCreateRequest`, `RepoAddRequest` TypeScript 类型定义
- [ ] `projectsAPI` 客户端覆盖所有 Projects API 端点
- [ ] `useProjectStore` Pinia store 实现 fetchProjects/fetchProject/createProject/deleteProject/addRepo
- [ ] ProjectList.vue 展示项目卡片网格 + 创建对话框
- [ ] ProjectDetail.vue 展示项目信息 + 仓库列表 + 添加仓库对话框
- [ ] `/projects` 路由配置（不含 ProjectEditor）+ `/sites` 重定向到 `/projects`
- [ ] 侧边栏从"站点管理/我的站点"更新为"项目管理/我的项目"
- [ ] `npm run build` 编译通过
