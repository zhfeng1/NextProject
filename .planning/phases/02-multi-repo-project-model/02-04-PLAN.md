---
phase: 2
plan: 4
title: "文件浏览与 Monaco 集成 — 多仓库 Tab 切换 + 只读编辑器"
wave: 4
depends_on:
  - PLAN-03-project-frontend
files_modified:
  - frontend/src/views/Projects/ProjectEditor.vue
  - frontend/src/views/Projects/components/RepoFileTree.vue
  - frontend/src/views/Projects/components/RepoTabs.vue
  - frontend/src/router/index.ts
autonomous: true
requirements:
  - PROJ-01
  - PROJ-02
  - PROJ-03
  - PROJ-04
  - PROJ-05
---

# Plan 04: 文件浏览与 Monaco 集成 — 多仓库 Tab 切换 + 只读编辑器

## Goal

创建 ProjectEditor 页面，实现多仓库 Tab 切换、文件树浏览、Monaco 编辑器多标签页打开文件（标签显示 `[repo_name] path`），切换仓库 Tab 时文件树刷新但已打开的编辑器标签保持不变。

**[ISSUE-05 说明]** PROJ-05 要求"文件浏览"为只读模式。ProjectEditor 中的 Monaco 编辑器以只读方式展示文件内容，不提供文件保存功能。如未来需要编辑保存能力，用户仍可通过现有 SiteEditor（`/sites/:id/edit`）操作单个仓库。本 Plan 不新增文件写入端点。

<threat_model>

## Threat Model

| Threat | Severity | Mitigation |
|--------|----------|------------|
| T-02-01: 文件路径穿越 | High | 后端 API 已有路径安全检查（Plan 02 实现），前端不自行拼接文件系统路径 |
| T-02-08: Monaco 内容注入 | Low | Monaco 编辑器以纯文本模式加载内容，不渲染 HTML |

**No blocking threats.**

</threat_model>

## Tasks

<task id="02-04-01">
<title>创建 RepoTabs 组件 — 仓库 Tab 切换</title>
<read_first>
- frontend/src/views/Sites/SiteEditor.vue (行 1-50, SiteEditor 组件导入模式和 script setup 结构)
- frontend/src/types/models.ts (Site 接口, status 字段)
</read_first>
<action>
新建 `frontend/src/views/Projects/components/RepoTabs.vue`：

```vue
<script setup lang="ts">
import type { Site } from '@/types/models'

const props = defineProps<{
  repos: Site[]
  activeRepoId: string
}>()

const emit = defineEmits<{
  (e: 'select', repoId: string): void
}>()
</script>

<template>
  <div class="flex border-b overflow-x-auto">
    <button
      v-for="repo in repos"
      :key="repo.site_id"
      class="px-4 py-2 text-sm whitespace-nowrap border-b-2 transition-colors"
      :class="activeRepoId === repo.site_id
        ? 'border-primary text-primary font-medium'
        : 'border-transparent text-muted-foreground hover:text-foreground'"
      @click="emit('select', repo.site_id)"
    >
      {{ repo.name }}
      <span
        v-if="repo.status === 'building'"
        class="ml-1 text-xs text-yellow-500"
      >克隆中...</span>
    </button>
  </div>
</template>
```

关键点：
- Props: `repos`（Site 数组）、`activeRepoId`（当前选中仓库的 site_id）
- Emits: `select` 事件，携带 repo 的 `site_id`
- 克隆中的仓库显示"克隆中..."提示（D-07）
</action>
<acceptance_criteria>
- `frontend/src/views/Projects/components/RepoTabs.vue` 文件存在
- 文件包含 `activeRepoId`
- 文件包含 `emit('select'`
- 文件包含 `克隆中`
</acceptance_criteria>
</task>

<task id="02-04-02">
<title>创建 RepoFileTree 组件 — 仓库文件树</title>
<read_first>
- frontend/src/views/Sites/SiteEditor.vue (搜索 "files" 相关逻辑, 文件树渲染方式)
- frontend/src/api/projects.ts (listRepoFiles 方法签名)
- frontend/src/components/SiteFileBrowserDialog.vue (如有, 文件树组件参考)
</read_first>
<action>
新建 `frontend/src/views/Projects/components/RepoFileTree.vue`：

```vue
<script setup lang="ts">
import { ref, watch } from 'vue'
import { projectsAPI } from '@/api/projects'
import { FolderOpen, File, ChevronRight, ChevronDown } from 'lucide-vue-next'

interface FileEntry {
  name: string
  path: string
  type: 'directory' | 'file'
  size?: number | null
}

const props = defineProps<{
  projectId: string
  repoId: string
  repoName: string
}>()

const emit = defineEmits<{
  (e: 'open-file', payload: { path: string; repoId: string; repoName: string }): void
}>()

const entries = ref<FileEntry[]>([])
const currentPath = ref('')
const loading = ref(false)
const expandedDirs = ref<Set<string>>(new Set())

async function loadFiles(path = '') {
  loading.value = true
  try {
    const res = await projectsAPI.listRepoFiles(props.projectId, props.repoId, path)
    entries.value = res.entries
    currentPath.value = path
  } finally {
    loading.value = false
  }
}

function handleClick(entry: FileEntry) {
  if (entry.type === 'directory') {
    if (expandedDirs.value.has(entry.path)) {
      expandedDirs.value.delete(entry.path)
    } else {
      expandedDirs.value.add(entry.path)
      loadFiles(entry.path)
    }
  } else {
    emit('open-file', {
      path: entry.path,
      repoId: props.repoId,
      repoName: props.repoName,
    })
  }
}

// 当仓库切换时重新加载根目录
watch(() => props.repoId, () => {
  entries.value = []
  expandedDirs.value.clear()
  loadFiles('')
}, { immediate: true })
</script>

<template>
  <div class="text-sm">
    <div v-if="loading" class="p-2 text-muted-foreground">加载中...</div>
    <div v-else>
      <div
        v-for="entry in entries"
        :key="entry.path"
        class="flex items-center gap-1 px-2 py-1 cursor-pointer hover:bg-accent rounded"
        @click="handleClick(entry)"
      >
        <component
          :is="entry.type === 'directory' ? FolderOpen : File"
          class="w-4 h-4 text-muted-foreground flex-shrink-0"
        />
        <span class="truncate">{{ entry.name }}</span>
      </div>
    </div>
  </div>
</template>
```

关键点：
- `watch(repoId)` 切换时自动刷新文件树（D-11）
- `emit('open-file')` 携带 `repoId` + `repoName` 供 ProjectEditor 打开编辑器标签
- 简单的单层文件列表（可后续扩展为递归树，v1 先实现基本功能）
</action>
<acceptance_criteria>
- `frontend/src/views/Projects/components/RepoFileTree.vue` 文件存在
- 文件包含 `projectsAPI.listRepoFiles`
- 文件包含 `emit('open-file'`
- 文件包含 `watch(() => props.repoId`
- 文件包含 `repoName`
</acceptance_criteria>
</task>

<task id="02-04-03">
<title>创建 ProjectEditor.vue 主页面 + 注册路由（只读浏览模式）</title>
<read_first>
- frontend/src/views/Sites/SiteEditor.vue (完整文件, Monaco 集成模式 — 编辑器初始化、文件加载)
- frontend/src/views/Projects/components/RepoTabs.vue (新建的 Tab 组件)
- frontend/src/views/Projects/components/RepoFileTree.vue (新建的文件树组件)
- frontend/src/stores/project.ts (fetchProject 方法)
- frontend/src/api/projects.ts (getRepoFile 方法)
- frontend/src/router/index.ts (当前路由配置, PLAN-03 已添加 ProjectList/ProjectDetail 路由)
</read_first>
<action>
**步骤 1：创建 ProjectEditor.vue**

新建 `frontend/src/views/Projects/ProjectEditor.vue`：

核心结构：
```vue
<script setup lang="ts">
import { ref, computed, onMounted, shallowRef } from 'vue'
import { useRoute, useRouter } from 'vue-router'
import { useProjectStore } from '@/stores/project'
import { projectsAPI } from '@/api/projects'
import RepoTabs from './components/RepoTabs.vue'
import RepoFileTree from './components/RepoFileTree.vue'
import type { Site } from '@/types/models'

const route = useRoute()
const router = useRouter()
const projectStore = useProjectStore()
const projectId = route.params.id as string

// 当前选中的仓库
const activeRepoId = ref('')

// 已打开的编辑器标签（只读浏览）
interface EditorTab {
  id: string          // 唯一 key: `${repoId}:${path}`
  label: string       // 显示: `[repoName] filename`
  repoId: string
  repoName: string
  path: string
  content: string
  language: string
}
const openTabs = ref<EditorTab[]>([])
const activeTabId = ref('')

const repos = computed<Site[]>(() => projectStore.currentProject?.repos || [])
const activeRepo = computed(() => repos.value.find(r => r.site_id === activeRepoId.value))

onMounted(async () => {
  await projectStore.fetchProject(projectId)
  if (repos.value.length > 0) {
    activeRepoId.value = repos.value[0].site_id
  }
})

function handleSelectRepo(repoId: string) {
  activeRepoId.value = repoId
  // 切换仓库不关闭已打开标签 (D-12)
}

function detectLanguage(filename: string): string {
  const ext = filename.split('.').pop()?.toLowerCase() || ''
  const map: Record<string, string> = {
    ts: 'typescript', tsx: 'typescriptreact', js: 'javascript', jsx: 'javascriptreact',
    py: 'python', vue: 'html', html: 'html', css: 'css', scss: 'scss',
    json: 'json', md: 'markdown', yaml: 'yaml', yml: 'yaml',
    sh: 'shell', bash: 'shell', sql: 'sql', dockerfile: 'dockerfile',
  }
  return map[ext] || 'plaintext'
}

async function handleOpenFile(payload: { path: string; repoId: string; repoName: string }) {
  const tabId = `${payload.repoId}:${payload.path}`

  // 已打开则切换到该标签
  const existing = openTabs.value.find(t => t.id === tabId)
  if (existing) {
    activeTabId.value = tabId
    return
  }

  // 从 API 加载文件内容（只读）
  const res = await projectsAPI.getRepoFile(projectId, payload.repoId, payload.path)
  const filename = payload.path.split('/').pop() || payload.path

  const tab: EditorTab = {
    id: tabId,
    label: `[${payload.repoName}] ${filename}`,
    repoId: payload.repoId,
    repoName: payload.repoName,
    path: payload.path,
    content: res.content || '',
    language: detectLanguage(filename),
  }
  openTabs.value.push(tab)
  activeTabId.value = tabId
}

function handleCloseTab(tabId: string) {
  openTabs.value = openTabs.value.filter(t => t.id !== tabId)
  if (activeTabId.value === tabId) {
    activeTabId.value = openTabs.value.length > 0 ? openTabs.value[openTabs.value.length - 1].id : ''
  }
}

const activeTabContent = computed(() => openTabs.value.find(t => t.id === activeTabId.value))
</script>

<template>
  <div class="flex flex-col h-full">
    <!-- 仓库 Tabs -->
    <RepoTabs
      :repos="repos"
      :activeRepoId="activeRepoId"
      @select="handleSelectRepo"
    />

    <div class="flex flex-1 overflow-hidden">
      <!-- 左侧文件树 -->
      <div class="w-64 border-r overflow-y-auto p-2" v-if="activeRepo">
        <RepoFileTree
          :projectId="projectId"
          :repoId="activeRepoId"
          :repoName="activeRepo.name"
          @open-file="handleOpenFile"
        />
      </div>

      <!-- 右侧编辑区（只读浏览） -->
      <div class="flex-1 flex flex-col overflow-hidden">
        <!-- 编辑器标签栏 -->
        <div class="flex border-b overflow-x-auto bg-muted/30" v-if="openTabs.length">
          <button
            v-for="tab in openTabs"
            :key="tab.id"
            class="px-3 py-1.5 text-xs whitespace-nowrap border-r flex items-center gap-1"
            :class="activeTabId === tab.id ? 'bg-background font-medium' : 'text-muted-foreground hover:bg-background/50'"
            @click="activeTabId = tab.id"
          >
            {{ tab.label }}
            <span class="ml-1 hover:text-destructive" @click.stop="handleCloseTab(tab.id)">&times;</span>
          </button>
        </div>

        <!-- Monaco 编辑器区域（只读） -->
        <div class="flex-1 overflow-hidden" v-if="activeTabContent">
          <!-- [ISSUE-05] Monaco Editor 以 readOnly 模式渲染 -->
          <!-- 使用 @monaco-editor/vue 或 vue-monaco-editor，设置 options.readOnly = true -->
          <!-- value: activeTabContent.content, language: activeTabContent.language, readOnly: true -->
          <div class="p-4 text-sm font-mono whitespace-pre-wrap overflow-auto h-full">
            {{ activeTabContent.content }}
          </div>
        </div>
        <div v-else class="flex-1 flex items-center justify-center text-muted-foreground">
          选择文件查看内容
        </div>
      </div>
    </div>
  </div>
</template>
```

**步骤 2：注册 ProjectEditor 路由**

**[NEW-02 修复]** 修改 `frontend/src/router/index.ts`，在 PLAN-03 已添加的 `projects/:id` (ProjectDetail) 路由之后添加：
```typescript
{
  path: 'projects/:id/edit',
  name: 'ProjectEditor',
  component: () => import('@/views/Projects/ProjectEditor.vue'),
},
```

将路由注册与组件创建放在同一个 Plan 中，确保 Vite 构建时动态 import 的文件一定存在，`npm run build` 不会因找不到模块而失败。

注意：
- **[ISSUE-05 修复]** Monaco Editor 以 `readOnly: true` 模式加载，不提供保存功能。此处先用 `<pre>` 占位显示文件内容，后续替换为与 SiteEditor 相同的 Monaco 组件时需设置 `readOnly: true`。
- 标签格式 `[repoName] filename` 满足 D-12 要求
- 切换仓库 Tab 不关闭已打开标签（D-12）
- 文件树只显示当前仓库文件（D-11），由 RepoFileTree 的 `watch(repoId)` 实现
- 如用户需要编辑保存功能，可使用现有 SiteEditor（`/sites/:id/edit`）操作单个仓库
</action>
<acceptance_criteria>
- `frontend/src/views/Projects/ProjectEditor.vue` 文件存在
- 文件包含 `<script setup lang="ts">`
- 文件包含 `RepoTabs`
- 文件包含 `RepoFileTree`
- 文件包含 `interface EditorTab`
- 文件包含 `activeRepoId`
- 文件包含 `openTabs`
- 文件包含 `handleOpenFile`
- 文件包含 `handleCloseTab`
- 文件包含 `[${payload.repoName}]` (标签格式)
- 文件包含 `readOnly` (只读模式注释)
- `frontend/src/router/index.ts` 包含 `name: 'ProjectEditor'`
- `frontend/src/router/index.ts` 包含 `import('@/views/Projects/ProjectEditor.vue')`
- `cd frontend && npm run build` 编译通过
</acceptance_criteria>
</task>

## Verification

```bash
# 1. 前端编译通过
cd frontend && npm run build

# 2. 后端测试仍通过
cd backend && python -m pytest tests/ -v

# 3. 关键文件存在
ls frontend/src/views/Projects/ProjectEditor.vue
ls frontend/src/views/Projects/components/RepoTabs.vue
ls frontend/src/views/Projects/components/RepoFileTree.vue

# 4. 关键内容验证
grep "activeRepoId" frontend/src/views/Projects/ProjectEditor.vue
grep "RepoTabs" frontend/src/views/Projects/ProjectEditor.vue
grep "open-file" frontend/src/views/Projects/components/RepoFileTree.vue
grep "克隆中" frontend/src/views/Projects/components/RepoTabs.vue
grep "readOnly" frontend/src/views/Projects/ProjectEditor.vue
grep "ProjectEditor" frontend/src/router/index.ts
```

## must_haves

- [ ] RepoTabs 组件实现仓库 Tab 切换，克隆中仓库显示提示
- [ ] RepoFileTree 组件实现文件树浏览，仓库切换时自动刷新
- [ ] ProjectEditor 页面集成 RepoTabs + RepoFileTree + 编辑器标签栏
- [ ] ProjectEditor 路由注册在本 Plan 中完成（与组件同步创建，避免构建失败）
- [ ] 编辑器标签格式为 `[repo_name] filename`
- [ ] 切换仓库 Tab 不关闭已打开的编辑器标签
- [ ] Monaco 编辑器为只读模式（PROJ-05 为浏览功能，编辑保留在 SiteEditor）
- [ ] `npm run build` 编译通过
