---
phase: 2
plan: 5
title: "Phase 2 UAT Gap Closure — clone_repo 注册、仓库删除、编辑器改进"
wave: 5
depends_on:
  - PLAN-04-monaco-editor
files_modified:
  - backend/core/celery_app.py
  - backend/tasks/__init__.py
  - backend/services/project_service.py
  - backend/api/v2/projects.py
  - frontend/src/views/Projects/ProjectDetail.vue
  - frontend/src/views/Projects/ProjectEditor.vue
  - frontend/src/api/projects.ts
  - frontend/src/stores/project.ts
autonomous: true
requirements:
  - PROJ-03
  - PROJ-04
  - PROJ-05
must_haves:
  - clone_repo_task 注册到 Celery worker（git clone 能实际执行）
  - 仓库支持单独删除（后端 API + 前端 UI）
  - 编辑器页面有返回按钮
  - 空白仓库不生成默认模板代码
  - 文件查看使用 Monaco 编辑器（只读 + 语法高亮）
  - 添加仓库表单必填项有星号标记
---

# Plan 05: Phase 2 UAT Gap Closure

## Goal

修复 Phase 2 UAT 中发现的 6 个问题：1 个 major（clone_repo 未注册）、3 个 minor（返回按钮、默认模板、仓库删除）、2 个 cosmetic（必填标记、语法高亮）。

## Tasks

<task id="02-05-01" wave="1">
<title>将 clone_repo_task 注册到 Celery worker</title>
<read_first>
- backend/core/celery_app.py
- backend/tasks/__init__.py
- backend/tasks/clone_repo.py
</read_first>
<action>
**步骤 1：修改 `backend/core/celery_app.py`**

在 `include` 列表中添加 `"backend.tasks.clone_repo"`：

```python
celery_app = Celery(
    "nextproject",
    broker=settings.celery_broker_url,
    backend=settings.celery_result_backend,
    include=[
        "backend.tasks.develop_code",
        "backend.tasks.deploy",
        "backend.tasks.test",
        "backend.tasks.clone_repo",
    ],
)
```

在 `task_routes` 字典中添加 clone_repo 路由：

```python
task_routes={
    "backend.tasks.develop_code.develop_code_task": {"queue": "ai-tasks"},
    "backend.tasks.deploy.deploy_task": {"queue": "deploy-tasks"},
    "backend.tasks.test.playwright_smoke_task": {"queue": "test-tasks"},
    "backend.tasks.clone_repo.clone_repo_task": {"queue": "default"},
},
```

**步骤 2：修改 `backend/tasks/__init__.py`**

添加 clone_repo_task 的导入和导出：

```python
from backend.tasks.deploy import deploy_task
from backend.tasks.develop_code import develop_code_task
from backend.tasks.test import smoke_test_task
from backend.tasks.clone_repo import clone_repo_task

__all__ = ["deploy_task", "develop_code_task", "smoke_test_task", "clone_repo_task"]
```
</action>
<acceptance_criteria>
- `grep "backend.tasks.clone_repo" backend/core/celery_app.py` 有输出
- `grep "clone_repo_task" backend/core/celery_app.py` 有输出
- `grep "clone_repo_task" backend/tasks/__init__.py` 有输出
- 重启 worker 后 `celery -A backend.core.celery_app inspect registered` 包含 clone_repo_task
</acceptance_criteria>
</task>

<task id="02-05-02" wave="1">
<title>新增仓库删除 API 端点</title>
<read_first>
- backend/api/v2/projects.py
- backend/services/project_service.py
- backend/services/site_service.py (delete 相关方法)
</read_first>
<action>
**步骤 1：在 `backend/services/project_service.py` 添加 `delete_repo` 方法**

在 `add_repo` 方法之后添加：

```python
async def delete_repo(
    self,
    db: AsyncSession,
    project_id: str,
    repo_id: str,
    current_user: object,
) -> None:
    """Delete a single repo from a project."""
    project = await self.get_project(db, project_id, current_user)
    from backend.services.site_service import site_service
    site = await site_service.get_site_by_public_id(db, repo_id, current_user)
    if str(site.project_id) != str(project_id):
        raise HTTPException(status_code=404, detail="Repo not found in this project")
    from datetime import datetime, timezone
    site.deleted_at = datetime.now(timezone.utc)
    await db.commit()
    # 清理磁盘上的仓库目录
    import shutil
    repo_dir = self.repo_root(project_id, site.name)
    if repo_dir.exists():
        shutil.rmtree(repo_dir, ignore_errors=True)
```

**步骤 2：在 `backend/api/v2/projects.py` 添加 DELETE 端点**

在 `get_repo_file` 端点之前添加：

```python
@router.delete("/{project_id}/repos/{repo_id}")
async def delete_repo(
    project_id: str,
    repo_id: str,
    current_user: object = Depends(get_current_user),
    db: AsyncSession = Depends(get_db),
) -> dict[str, Any]:
    await project_service.delete_repo(db, project_id, repo_id, current_user)
    return {"ok": True}
```
</action>
<acceptance_criteria>
- `grep "delete_repo" backend/services/project_service.py` 有输出
- `grep "delete_repo" backend/api/v2/projects.py` 有输出
- `grep "DELETE" backend/api/v2/projects.py` 或 `grep "@router.delete.*repos" backend/api/v2/projects.py` 有输出
</acceptance_criteria>
</task>

<task id="02-05-03" wave="1">
<title>空白仓库使用简化初始化（不生成默认模板代码）</title>
<read_first>
- backend/services/project_service.py (add_repo 方法中 ensure_site_structure 调用, 行 216-219)
- backend/services/site_service.py (ensure_site_structure 方法, 行 289-319)
</read_first>
<action>
**修改 `backend/services/project_service.py` 的 `add_repo` 方法**

将 else 分支中的 `ensure_site_structure` 替换为仅创建目录和初始化 git：

找到以下代码块（约行 215-219）：
```python
        else:
            # Blank repo: initialize at project grouped path
            repo_path = self.repo_root(project_id, repo_name)
            site_service.ensure_site_structure(site.site_id, override_root=repo_path)
            await db.commit()
```

替换为：
```python
        else:
            # Blank repo: create empty directory with git init (no template files)
            repo_path = self.repo_root(project_id, repo_name)
            repo_path.mkdir(parents=True, exist_ok=True)
            import subprocess, shutil
            git_bin = shutil.which("git")
            if git_bin and not (repo_path / ".git").exists():
                subprocess.run([git_bin, "init"], cwd=str(repo_path), capture_output=True)
            await db.commit()
```

这样空白仓库只有一个空的 git 目录，不再包含 backend/app.py、frontend/index.html 等默认模板文件。
</action>
<acceptance_criteria>
- `grep "no template files" backend/services/project_service.py` 有输出
- `grep -v "ensure_site_structure" backend/services/project_service.py | grep -c "repo_path.mkdir"` 输出大于 0（确认使用 mkdir 而非 ensure_site_structure）
</acceptance_criteria>
</task>

<task id="02-05-04" wave="2">
<title>前端：仓库删除按钮 + API 调用 + Store 方法</title>
<read_first>
- frontend/src/views/Projects/ProjectDetail.vue
- frontend/src/api/projects.ts
- frontend/src/stores/project.ts
</read_first>
<action>
**步骤 1：在 `frontend/src/api/projects.ts` 添加 deleteRepo 方法**

在 `addRepo` 方法之后添加：

```typescript
deleteRepo(projectId: string, repoId: string) {
  return client.delete<any, { ok: boolean }>(`/projects/${projectId}/repos/${repoId}`)
},
```

**步骤 2：在 `frontend/src/stores/project.ts` 添加 deleteRepo action**

在 `addRepo` action 之后添加：

```typescript
async deleteRepo(projectId: string, repoId: string) {
  await projectsAPI.deleteRepo(projectId, repoId)
  if (this.currentProject?.id === projectId) {
    await this.fetchProject(projectId)
  }
},
```

**步骤 3：在 `frontend/src/views/Projects/ProjectDetail.vue` 添加删除按钮**

3a. 在 script setup 的 import 中添加 `Trash2` 图标：
```typescript
import { Plus, ArrowLeft, GitBranch, Code, Trash2 } from 'lucide-vue-next'
```

3b. 添加删除仓库的处理函数（在 handleAddRepo 之后）：
```typescript
const handleDeleteRepo = async (repoId: string, repoName: string) => {
  if (!confirm(`确认删除仓库「${repoName}」？此操作不可恢复。`)) return
  try {
    await projectStore.deleteRepo(projectId, repoId)
    toast.success('仓库已删除')
  } catch {
    toast.error('删除仓库失败')
  }
}
```

3c. 在仓库卡片的 `<CardContent>` 内的 status span 下方添加删除按钮：
```vue
<Button variant="ghost" size="sm" class="text-destructive mt-2" @click.stop="handleDeleteRepo(repo.site_id, repo.name)">
  <Trash2 class="w-4 h-4 mr-1" />
  删除
</Button>
```
</action>
<acceptance_criteria>
- `grep "deleteRepo" frontend/src/api/projects.ts` 有输出
- `grep "deleteRepo" frontend/src/stores/project.ts` 有输出
- `grep "handleDeleteRepo" frontend/src/views/Projects/ProjectDetail.vue` 有输出
- `grep "Trash2" frontend/src/views/Projects/ProjectDetail.vue` 有输出
</acceptance_criteria>
</task>

<task id="02-05-05" wave="2">
<title>编辑器页面添加返回按钮</title>
<read_first>
- frontend/src/views/Projects/ProjectEditor.vue
- frontend/src/views/Projects/ProjectDetail.vue (参考其返回按钮模式, 行 82-86)
</read_first>
<action>
**修改 `frontend/src/views/Projects/ProjectEditor.vue`**

1. 在 script setup 的 import 中添加 ArrowLeft 和 Button：
```typescript
import { ArrowLeft } from 'lucide-vue-next'
import { Button } from '@/components/ui/button'
```

2. 在 template 的 `<div class="flex flex-col h-full">` 内、RepoTabs 之前添加返回栏：
```vue
<!-- 顶部导航栏 -->
<div class="flex items-center gap-2 px-4 py-2 border-b bg-muted/30">
  <Button variant="ghost" size="sm" @click="router.push(`/projects/${projectId}`)">
    <ArrowLeft class="w-4 h-4 mr-1" />
    返回项目
  </Button>
  <span class="text-sm font-medium" v-if="projectStore.currentProject">
    {{ projectStore.currentProject.name }}
  </span>
</div>
```
</action>
<acceptance_criteria>
- `grep "ArrowLeft" frontend/src/views/Projects/ProjectEditor.vue` 有输出
- `grep "返回项目" frontend/src/views/Projects/ProjectEditor.vue` 有输出
- `grep "router.push" frontend/src/views/Projects/ProjectEditor.vue` 有输出
</acceptance_criteria>
</task>

<task id="02-05-06" wave="2">
<title>文件查看使用 Monaco 编辑器（只读 + 语法高亮）</title>
<read_first>
- frontend/src/views/Projects/ProjectEditor.vue (行 148-152, 当前 pre 占位)
- frontend/src/components/Editor/CodeEditor.vue (已有 Monaco 组件, 支持 readonly prop)
</read_first>
<action>
**修改 `frontend/src/views/Projects/ProjectEditor.vue`**

1. 在 script setup 中添加 CodeEditor 导入：
```typescript
import CodeEditor from '@/components/Editor/CodeEditor.vue'
```

2. 替换文件内容显示区域。找到以下代码块：
```vue
<div class="flex-1 overflow-hidden" v-if="activeTabContent">
  <div class="p-4 text-sm font-mono whitespace-pre-wrap overflow-auto h-full">
    {{ activeTabContent.content }}
  </div>
</div>
```

替换为：
```vue
<div class="flex-1 overflow-hidden" v-if="activeTabContent">
  <CodeEditor
    :modelValue="activeTabContent.content"
    :language="activeTabContent.language"
    :readonly="true"
    theme="vs-dark"
  />
</div>
```
</action>
<acceptance_criteria>
- `grep "CodeEditor" frontend/src/views/Projects/ProjectEditor.vue` 有输出
- `grep "readonly" frontend/src/views/Projects/ProjectEditor.vue` 有输出
- `grep -c "whitespace-pre-wrap" frontend/src/views/Projects/ProjectEditor.vue` 输出为 0（pre 占位已移除）
</acceptance_criteria>
</task>

<task id="02-05-07" wave="2">
<title>添加仓库表单必填项星号标记</title>
<read_first>
- frontend/src/views/Projects/ProjectDetail.vue (行 130-133, 仓库名称 Label 和 Input)
</read_first>
<action>
**修改 `frontend/src/views/Projects/ProjectDetail.vue`**

找到添加仓库对话框中的仓库名称 Label（约行 131-133）：
```vue
<div>
  <Label>仓库名称</Label>
  <Input v-model="addRepoForm.name" placeholder="输入仓库名称" />
</div>
```

替换为：
```vue
<div>
  <Label>仓库名称 <span class="text-destructive">*</span></Label>
  <Input v-model="addRepoForm.name" placeholder="输入仓库名称" required />
</div>
```
</action>
<acceptance_criteria>
- `grep "text-destructive.*\*" frontend/src/views/Projects/ProjectDetail.vue` 有输出
- `grep "required" frontend/src/views/Projects/ProjectDetail.vue` 有输出
</acceptance_criteria>
</task>

## Verification

```bash
# 1. 后端 clone_repo 注册
grep "backend.tasks.clone_repo" backend/core/celery_app.py
grep "clone_repo_task" backend/tasks/__init__.py

# 2. 仓库删除 API
grep "delete_repo" backend/api/v2/projects.py
grep "delete_repo" backend/services/project_service.py

# 3. 空白仓库简化
grep "no template files" backend/services/project_service.py

# 4. 前端仓库删除
grep "deleteRepo" frontend/src/api/projects.ts
grep "handleDeleteRepo" frontend/src/views/Projects/ProjectDetail.vue

# 5. 编辑器返回按钮
grep "返回项目" frontend/src/views/Projects/ProjectEditor.vue

# 6. Monaco 语法高亮
grep "CodeEditor" frontend/src/views/Projects/ProjectEditor.vue

# 7. 必填标记
grep "text-destructive" frontend/src/views/Projects/ProjectDetail.vue

# 8. 前端编译
cd frontend && npm run build

# 9. 后端测试
cd backend && python -m pytest tests/ -v
```

## Wave Summary

| Wave | Tasks | Description |
|------|-------|-------------|
| 1 | 02-05-01, 02-05-02, 02-05-03 | 后端修复：Celery 注册、删除 API、空白仓库简化（无前端依赖，可并行） |
| 2 | 02-05-04, 02-05-05, 02-05-06, 02-05-07 | 前端改进：仓库删除 UI、返回按钮、Monaco 高亮、必填标记（02-05-04 依赖 02-05-02 的 API） |
