---
wave: 1
depends_on: []
files_modified:
  - backend/core/config.py
  - backend/core/encryption.py
  - backend/api/v2/providers.py
  - backend/alembic/versions/20260423_0001_encrypt_api_keys.py
  - .env.example
  - docker-compose.yml
  - frontend/src/views/Settings/Account.vue
autonomous: true
requirements:
  - SEC-01
  - SEC-02
  - SEC-03
  - KEY-01
  - KEY-02
  - KEY-03
  - KEY-04
---

# Plan 01: API Key 加密存储改造

## Goal

将 `UserLLMProvider.api_key` 从明文存储改为 Fernet 加密存储，API 返回时脱敏处理，迁移现有明文数据。这是 Phase 1 的基础设施 Plan，后续 Plan 02 和 Plan 03 依赖本 Plan 提供的加密/解密/脱敏函数。

<threat_model>

### Assets
- **A1**: 用户 API Key（Claude/OpenAI），存储在 `user_llm_providers.api_key` 列
- **A2**: FERNET_KEY 加密主密钥，存储在 `.env` / 环境变量中

### Threats
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T1 | 数据库泄露导致 API Key 明文暴露 | **HIGH** | Fernet 加密所有 api_key，数据库中仅存密文 |
| T2 | API 响应返回明文 Key 被中间人/日志捕获 | HIGH | `_serialize()` 始终返回脱敏值 `sk-****abcd` |
| T3 | FERNET_KEY 泄露导致所有 Key 可解密 | HIGH | 环境变量注入，不落代码库；docker-compose 使用 `:?required` 语法强制设置 |
| T4 | 脱敏值 `****` 被误写入数据库覆盖真实 Key | MEDIUM | update 端点检测 `****` 模式，跳过 api_key 更新 |
| T5 | FERNET_KEY 丢失导致数据不可恢复 | MEDIUM | `.env.example` 注释明确提醒备份；校验器防止空值 |
| T6 | 迁移脚本重复加密已加密数据 | LOW | 检测 `gAAAAA` 前缀跳过已加密行 |

### Accepted Risks
- FERNET_KEY 存储在服务器环境变量中，服务器被完全攻陷时 Key 可被提取（属于基础设施安全范畴，非应用层能解决）

</threat_model>

## Tasks

<task id="1.1">
<title>添加 FERNET_KEY 配置项</title>
<read_first>
- backend/core/config.py
- .env.example
- docker-compose.yml
</read_first>
<action>
1. 在 `backend/core/config.py` 的 `Settings` 类中，在 `secret_key` 字段（L35）之后添加：
```python
fernet_key: str = Field(alias="FERNET_KEY")
```

2. 在 `validate_secret_key` 校验器（L92-103）之后，添加 fernet_key 校验器：
```python
@field_validator("fernet_key")
@classmethod
def validate_fernet_key(cls, value: str) -> str:
    value = value.strip()
    if not value or value == "replace-with-fernet-key":
        raise ValueError(
            "FERNET_KEY must be set. "
            'Generate one with: python3 -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"'
        )
    if len(value) != 44:
        raise ValueError("FERNET_KEY must be a valid Fernet key (44 characters, base64 encoded)")
    return value
```

3. 在 `.env.example` 的 `SECRET_KEY` 行（L20）之后添加：
```
# 加密密钥（Fernet），用于 API Key 加密存储，丢失不可恢复！请务必备份
# 生成命令：python3 -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
FERNET_KEY=replace-with-fernet-key
```

4. 在 `docker-compose.yml` 的 `main-service.environment` 中，`SECRET_KEY` 行（L77）之后添加：
```yaml
- FERNET_KEY=${FERNET_KEY:?FERNET_KEY is required}
```

5. 在 `docker-compose.yml` 的 `celery-worker.environment` 中，`SECRET_KEY` 行（L142）之后添加：
```yaml
- FERNET_KEY=${FERNET_KEY:?FERNET_KEY is required}
```

6. 在 `docker-compose.yml` 的 `test.environment` 中，`SECRET_KEY` 行（L238）之后添加：
```yaml
- FERNET_KEY=${FERNET_KEY:-dGVzdC1mZXJuZXQta2V5LWZvci10ZXN0aW5nMTIzNDU=}
```
（测试环境使用固定 key，无需 `:?required`）
</action>
<acceptance_criteria>
- `backend/core/config.py` contains `fernet_key: str = Field(alias="FERNET_KEY")`
- `backend/core/config.py` contains `def validate_fernet_key(cls, value: str)`
- `backend/core/config.py` contains `len(value) != 44`
- `.env.example` contains `FERNET_KEY=replace-with-fernet-key`
- `.env.example` contains `丢失不可恢复`
- `docker-compose.yml` contains `FERNET_KEY=${FERNET_KEY:?FERNET_KEY is required}` (at least 2 occurrences: main-service + celery-worker)
</acceptance_criteria>
</task>

<task id="1.2">
<title>创建加密工具模块 encryption.py</title>
<read_first>
- backend/core/security.py
- backend/core/config.py
</read_first>
<action>
创建 `backend/core/encryption.py`，内容如下：

```python
"""Fernet encryption utilities for API Key storage."""
from __future__ import annotations

from functools import lru_cache

from cryptography.fernet import Fernet

from backend.core.config import get_settings


@lru_cache(maxsize=1)
def _get_fernet() -> Fernet:
    """Lazy-init Fernet instance, cached after first call.

    Using lru_cache instead of module-level initialization so that
    importing this module does not trigger Settings loading — this
    keeps Alembic, tests, and other lightweight imports safe.
    """
    return Fernet(get_settings().fernet_key.encode())


def encrypt_api_key(plaintext: str) -> str:
    """Encrypt an API key. Returns empty string for empty input."""
    if not plaintext:
        return ""
    return _get_fernet().encrypt(plaintext.encode()).decode()


def decrypt_api_key(ciphertext: str) -> str:
    """Decrypt an API key. Returns empty string for empty input."""
    if not ciphertext:
        return ""
    return _get_fernet().decrypt(ciphertext.encode()).decode()


def mask_api_key(key: str) -> str:
    """Mask an API key for display: 'sk-****abcd' or '****abcd'.

    Returns empty string for empty input, passes through already-masked values.
    """
    if not key or "****" in key:
        return key
    prefix = ""
    dash_pos = key.find("-")
    if 0 < dash_pos <= 10:
        prefix = key[: dash_pos + 1]
    suffix = key[-4:] if len(key) >= 4 else key
    return f"{prefix}****{suffix}"


def is_masked(value: str) -> bool:
    """Check if a value is a masked API key (contains '****')."""
    return bool(value) and "****" in value


__all__ = [
    "decrypt_api_key",
    "encrypt_api_key",
    "is_masked",
    "mask_api_key",
]
```
</action>
<acceptance_criteria>
- `backend/core/encryption.py` exists
- `backend/core/encryption.py` contains `from cryptography.fernet import Fernet`
- `backend/core/encryption.py` contains `def encrypt_api_key(plaintext: str) -> str:`
- `backend/core/encryption.py` contains `def decrypt_api_key(ciphertext: str) -> str:`
- `backend/core/encryption.py` contains `def mask_api_key(key: str) -> str:`
- `backend/core/encryption.py` contains `def is_masked(value: str) -> bool:`
- `backend/core/encryption.py` contains `def _get_fernet() -> Fernet:`
- `backend/core/encryption.py` contains `@lru_cache(maxsize=1)`
</acceptance_criteria>
</task>

<task id="1.3">
<title>改造 providers.py — 加密写入 + 脱敏返回 + 脱敏值跳过</title>
<read_first>
- backend/api/v2/providers.py
- backend/core/encryption.py (task 1.2 创建的)
</read_first>
<action>
修改 `backend/api/v2/providers.py`：

1. 在文件头部 import 区域添加：
```python
from backend.core.encryption import decrypt_api_key, encrypt_api_key, is_masked, mask_api_key
```

2. 修改 `_serialize()` 函数（L17-29），将 `"api_key": p.api_key` 改为：
```python
"api_key": mask_api_key(decrypt_api_key(p.api_key)) if p.api_key else "",
```

3. 修改 `create_provider()` 函数（L51-58），将 `api_key=(payload.get("api_key") or "").strip()` 改为：
```python
api_key=encrypt_api_key((payload.get("api_key") or "").strip()),
```

4. 修改 `update_provider()` 函数（L78-81），将 allowed set + for 循环替换为：
```python
allowed = {"name", "base_url", "api_key", "models", "format", "is_default"}
for key, value in payload.items():
    if key not in allowed:
        continue
    if key == "api_key":
        raw_key = (value or "").strip()
        if not raw_key or is_masked(raw_key):
            continue  # skip masked or empty api_key — user didn't change it
        setattr(p, key, encrypt_api_key(raw_key))
    else:
        setattr(p, key, value)
```
</action>
<acceptance_criteria>
- `backend/api/v2/providers.py` contains `from backend.core.encryption import decrypt_api_key, encrypt_api_key, is_masked, mask_api_key`
- `backend/api/v2/providers.py` contains `mask_api_key(decrypt_api_key(p.api_key))`
- `backend/api/v2/providers.py` contains `encrypt_api_key((payload.get("api_key") or "").strip())`
- `backend/api/v2/providers.py` contains `if not raw_key or is_masked(raw_key):`
- `backend/api/v2/providers.py` contains `setattr(p, key, encrypt_api_key(raw_key))`
</acceptance_criteria>
</task>

<task id="1.4">
<title>创建 Alembic 数据迁移 — 明文转密文</title>
<read_first>
- backend/alembic/versions/20260331_0001_phase_2_features.py
- backend/alembic/versions/20260323_0004_add_user_llm_providers.py
</read_first>
<action>
**前置检查：** 执行前先运行 `ls backend/alembic/versions/` 确认当前最新迁移文件，确保 `down_revision` 指向实际的 head。当前已确认 head 为 `20260331_0001`。

创建 `backend/alembic/versions/20260423_0001_encrypt_api_keys.py`：

```python
"""Encrypt existing plaintext API keys with Fernet.

Revision ID: 20260423_0001
Revises: 20260331_0001
"""
from __future__ import annotations

import os

from alembic import op
import sqlalchemy as sa
from cryptography.fernet import Fernet

revision = "20260423_0001"
down_revision = "20260331_0001"
branch_labels = None
depends_on = None


def _get_fernet() -> Fernet:
    key = os.environ.get("FERNET_KEY", "")
    if not key:
        raise RuntimeError("FERNET_KEY environment variable is required for this migration")
    return Fernet(key.encode())


def upgrade() -> None:
    bind = op.get_bind()
    fernet = _get_fernet()
    rows = bind.execute(
        sa.text("SELECT id, api_key FROM user_llm_providers WHERE api_key IS NOT NULL AND api_key != ''")
    )
    for row in rows:
        api_key = row[1]
        if api_key.startswith("gAAAAA"):
            continue  # already encrypted
        encrypted = fernet.encrypt(api_key.encode()).decode()
        bind.execute(
            sa.text("UPDATE user_llm_providers SET api_key = :key WHERE id = :id"),
            {"key": encrypted, "id": row[0]},
        )


def downgrade() -> None:
    bind = op.get_bind()
    fernet = _get_fernet()
    rows = bind.execute(
        sa.text("SELECT id, api_key FROM user_llm_providers WHERE api_key IS NOT NULL AND api_key != ''")
    )
    for row in rows:
        api_key = row[1]
        if not api_key.startswith("gAAAAA"):
            continue  # not encrypted
        try:
            decrypted = fernet.decrypt(api_key.encode()).decode()
            bind.execute(
                sa.text("UPDATE user_llm_providers SET api_key = :key WHERE id = :id"),
                {"key": decrypted, "id": row[0]},
            )
        except Exception:
            pass  # skip rows that fail to decrypt
```
</action>
<acceptance_criteria>
- `backend/alembic/versions/20260423_0001_encrypt_api_keys.py` exists
- File contains `revision = "20260423_0001"`
- File contains `down_revision = "20260331_0001"`
- File contains `from cryptography.fernet import Fernet`
- File contains `if api_key.startswith("gAAAAA"):`
- File contains `os.environ.get("FERNET_KEY", "")`
- File contains `def upgrade() -> None:`
- File contains `def downgrade() -> None:`
</acceptance_criteria>
</task>

<task id="1.5">
<title>前端适配脱敏 API Key 展示</title>
<read_first>
- frontend/src/views/Settings/Account.vue
</read_first>
<action>
修改 `frontend/src/views/Settings/Account.vue`：

1. 修改 L305 的 api_key Input 组件，将 `placeholder` 从 `"sk-... 或留空（如本地 Ollama）"` 改为：
```html
<Input v-model="p.api_key" type="password" placeholder="输入新 Key 可覆盖，留空保持不变" />
```

此改动确保用户理解：保存后看到的是脱敏值，如果不需要修改则不填写。

由于后端已经在 `_serialize()` 中返回脱敏值（如 `sk-****abcd`），`Object.assign(p, toUI(res.provider))` 会自动将 `p.api_key` 更新为脱敏值。前端 `saveProvider()` 发送此脱敏值到后端时，后端会通过 `is_masked()` 检测到 `****` 并跳过 api_key 更新，无需前端做额外处理。
</action>
<acceptance_criteria>
- `frontend/src/views/Settings/Account.vue` contains `输入新 Key 可覆盖，留空保持不变`
- `frontend/src/views/Settings/Account.vue` does NOT contain `sk-... 或留空（如本地 Ollama）`
</acceptance_criteria>
</task>

## Verification

```bash
# 1. 检查配置项
grep -q 'fernet_key: str = Field(alias="FERNET_KEY")' backend/core/config.py && echo "PASS: config field"
grep -q 'validate_fernet_key' backend/core/config.py && echo "PASS: config validator"

# 2. 检查加密模块
test -f backend/core/encryption.py && echo "PASS: encryption module exists"
grep -q 'def encrypt_api_key' backend/core/encryption.py && echo "PASS: encrypt function"
grep -q 'def decrypt_api_key' backend/core/encryption.py && echo "PASS: decrypt function"
grep -q 'def mask_api_key' backend/core/encryption.py && echo "PASS: mask function"
grep -q '_get_fernet' backend/core/encryption.py && echo "PASS: lazy init"

# 3. 检查 providers.py 改造
grep -q 'mask_api_key(decrypt_api_key' backend/api/v2/providers.py && echo "PASS: serialize masking"
grep -q 'encrypt_api_key' backend/api/v2/providers.py && echo "PASS: encrypt on write"
grep -q 'is_masked' backend/api/v2/providers.py && echo "PASS: masked value check"

# 4. 检查迁移文件
test -f backend/alembic/versions/20260423_0001_encrypt_api_keys.py && echo "PASS: migration exists"
grep -q 'gAAAAA' backend/alembic/versions/20260423_0001_encrypt_api_keys.py && echo "PASS: skip already encrypted"

# 5. 检查环境变量注入
grep -c 'FERNET_KEY' docker-compose.yml | grep -q '[2-9]' && echo "PASS: FERNET_KEY in docker-compose (2+ services)"
grep -q 'FERNET_KEY=replace-with-fernet-key' .env.example && echo "PASS: .env.example"

# 6. 检查前端
grep -q '输入新 Key 可覆盖' frontend/src/views/Settings/Account.vue && echo "PASS: frontend placeholder"
```

## must_haves

- [ ] `user_llm_providers.api_key` 新写入的值在数据库中是 Fernet 密文（以 `gAAAAA` 开头）
- [ ] API 返回的 `api_key` 字段始终是脱敏值（如 `sk-****abcd`），永不返回明文
- [ ] 更新 provider 时，如果 `api_key` 包含 `****` 则跳过更新，不会将脱敏值误写入数据库
- [ ] 现有明文数据通过 Alembic 迁移自动加密
- [ ] FERNET_KEY 通过环境变量配置，main-service 和 celery-worker 都能访问
